<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.9.5">Jekyll</generator><link href="https://victorangeloblancada.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://victorangeloblancada.github.io/" rel="alternate" type="text/html" /><updated>2026-07-23T21:48:07+08:00</updated><id>https://victorangeloblancada.github.io/feed.xml</id><title type="html">Victor Angelo Blancada</title><subtitle>Victor Angelo Blancada is a data scientist, AI developer, and strategy consultant with proven experience in improving operational efficiency through predictive modeling and AI-powered automation.</subtitle><entry><title type="html">The Recursion Turns on Itself: Karpathy’s AutoResearch and Bilevel Meta-Loops</title><link href="https://victorangeloblancada.github.io/blog/2026/07/23/the-recursion-turns-on-itself-autoresearch.html" rel="alternate" type="text/html" title="The Recursion Turns on Itself: Karpathy’s AutoResearch and Bilevel Meta-Loops" /><published>2026-07-23T00:00:00+08:00</published><updated>2026-07-23T00:00:00+08:00</updated><id>https://victorangeloblancada.github.io/blog/2026/07/23/the-recursion-turns-on-itself-autoresearch</id><content type="html" xml:base="https://victorangeloblancada.github.io/blog/2026/07/23/the-recursion-turns-on-itself-autoresearch.html"><![CDATA[<script type="text/javascript" async="" src="https://cdnjs.cloudflare.com/ajax/libs/mathjax/2.7.5/latest.js?config=TeX-MML-AM_CHTML">
</script>

<p>In <em>Gödel, Escher, Bach</em>, Douglas Hofstadter famously explored the concept of “strange loops”—phenomena in which, by moving upwards through a hierarchical system, one unexpectedly finds themselves back at the starting point. It is a concept that has fascinated logicians, mathematicians, and artists for decades because it suggests that self-reference can generate meaning out of purely mechanical rules. Yet, in computer science, our self-improving loops have historically been more of a conceptual joke than a practical reality.</p>

<p>There is a peculiar joke buried inside modern machine learning. The field’s most celebrated activity—scientific research—is, viewed sideways, one of the most mechanical things humans do. A researcher forms a hypothesis, edits code, runs an experiment, reads the metric, keeps or discards the change, and repeats the cycle. A good researcher gets through maybe ten of these iterations on a productive day, most of it spent waiting for a GPU. The obvious question, once large language models became reliable code editors, was why a human should be inside that loop at all. Andrej Karpathy’s answer, delivered in March 2026 as roughly 630 lines of Python, was simple: they shouldn’t be.</p>

<p>Karpathy’s repository, <em>AutoResearch</em>, is small on purpose. Three files matter. The file <code class="language-plaintext highlighter-rouge">prepare.py</code> handles data preparation and the evaluation harness, and the agent is forbidden from touching it. The file <code class="language-plaintext highlighter-rouge">train.py</code>—a single-file GPT trainer built on Muon and AdamW, derived from Karpathy’s nanochat—is fair game; the agent can modify the architecture, optimizer, batch size, and learning rate. The file <code class="language-plaintext highlighter-rouge">program.md</code> is the human-authored English brief that tells the agent what to explore and what constraints to respect. The loop itself is embarrassingly simple: propose a change to <code class="language-plaintext highlighter-rouge">train.py</code>, train for a fixed five-minute wall-clock budget, measure validation bits-per-byte, keep if better, revert if not, and repeat overnight.</p>

<hr />

<h2 id="the-ratchet-and-the-bilevel-formulation">The Ratchet and the Bilevel Formulation</h2>

<p>Karpathy calls it a “ratchet loop,” and the framing matters. This is emphatically not standard hyperparameter search. Tools like Optuna and Ray Tune walk a predefined grid; AutoResearch lets the agent modify arbitrary code, meaning the search space is whatever the language model can construct. Nor is it quite Google’s AlphaEvolve, which is closed and evolutionary. AutoResearch is a bet that a general-purpose model’s priors—its taste, absorbed from the entire machine learning literature during pretraining—make it a better proposer than any random search algorithm.</p>

<p>The empirical results were, for a 630-line repository, unreasonably good. Pointed at his own already-tuned nanochat, Karpathy’s agent ran ~700 experiments over two days and found ~20 genuine improvements, cutting time-to-GPT-2-quality by 11 percent. Shopify’s Tobi Lütke reported an overnight run that produced a ~19 percent quality gain at roughly half the parameter count. Karpathy himself joined Anthropic to lead its autoresearch team two months later (which is the corporate equivalent of the market voting with a very large checkbook).</p>

<p>Which brings us to the paper Qu and Lu uploaded to arXiv in March 2026 and revised in June—<em>“Bilevel Autoresearch: Meta-Autoresearching Itself.”</em> Their observation is the one that should have been obvious the moment AutoResearch shipped: if autoresearch is a form of research, then autoresearch can be applied to autoresearch. Every improvement to the loop so far—Karpathy’s single-track version, AIMing Lab’s multi-batch AutoResearch-Claw, EvoScientist’s persistent memory—had been produced by a human reading the previous system’s code, spotting a bottleneck, and writing new code. Could an LLM do that step, too?</p>

<p>Their architecture is a bilevel optimization loop. The inner loop is a standard Karpathy-style autoresearcher optimizing validation loss on a pretraining benchmark. The outer loop does something more interesting: it reads the inner loop’s code and execution traces, identifies bottlenecks in how the inner loop is searching, and generates new Python search mechanisms which it injects into the inner loop at runtime. Crucially, both loops run on the same LLM; the gains come from meta-scaffolding, not from a smarter base model.</p>

<p>Mathematically, this can be formulated as a bilevel optimization problem:</p>

\[\min_{\theta \in \Theta} \mathcal{L}_{\text{outer}}(\theta, w^*(\theta)) \quad \text{subject to} \quad w^*(\theta) = \arg\min_{w \in \mathcal{W}} \mathcal{L}_{\text{inner}}(w; \theta)\]

<p>where \(\theta\) represents the search mechanism generated by the outer loop (such as prompt instructions or search heuristics), \(w\) represents the neural network weights and optimization parameters of the inner model, and \(\mathcal{L}\) represents the validation loss.</p>

<hr />

<h2 id="simulating-the-bilevel-optimization-flow">Simulating the Bilevel Optimization Flow</h2>

<p>To understand the architecture, we can sketch the control flow of a bilevel autoresearch loop. We can model the logic in pseudocode representing a Dify-style workflow, illustrating how the outer loop meta-optimizes the inner loop’s code and variables:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Simulating the outer loop: Meta-optimizing the inner search mechanism
</span><span class="k">def</span> <span class="nf">outer_loop_step</span><span class="p">(</span><span class="n">inner_loop_history</span><span class="p">,</span> <span class="n">current_search_mechanism</span><span class="p">):</span>
    <span class="s">"""Analyze the inner loop's search history to propose a better search mechanism.
    inner_loop_history: List of dictionaries containing inner loop execution traces.
    current_search_mechanism: The current search strategy code/prompt.
    """</span>
    <span class="k">print</span><span class="p">(</span><span class="s">"Outer Loop: Analyzing inner loop execution traces for search bottlenecks..."</span><span class="p">)</span>
    
    <span class="c1"># Identify bottlenecks (e.g., inner loop getting stuck in local minima)
</span>    <span class="n">losses</span> <span class="o">=</span> <span class="p">[</span><span class="n">trace</span><span class="p">[</span><span class="s">'val_loss'</span><span class="p">]</span> <span class="k">for</span> <span class="n">trace</span> <span class="ow">in</span> <span class="n">inner_loop_history</span><span class="p">]</span>
    <span class="k">if</span> <span class="nb">len</span><span class="p">(</span><span class="n">losses</span><span class="p">)</span> <span class="o">&gt;</span> <span class="mi">5</span> <span class="ow">and</span> <span class="nb">len</span><span class="p">(</span><span class="nb">set</span><span class="p">(</span><span class="n">losses</span><span class="p">[</span><span class="o">-</span><span class="mi">5</span><span class="p">:]))</span> <span class="o">==</span> <span class="mi">1</span><span class="p">:</span>
        <span class="n">diagnosis</span> <span class="o">=</span> <span class="s">"Inner loop is trapped in deterministic exploration."</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="n">diagnosis</span> <span class="o">=</span> <span class="s">"Inner loop is search-efficient but could explore wider architectures."</span>
        
    <span class="c1"># Propose a mechanism-level modification based on the diagnosis
</span>    <span class="k">if</span> <span class="s">"deterministic"</span> <span class="ow">in</span> <span class="n">diagnosis</span><span class="p">:</span>
        <span class="c1"># Inject Thompson-sampling or temperature scaling to force exploration
</span>        <span class="n">new_search_mechanism</span> <span class="o">=</span> <span class="n">current_search_mechanism</span> <span class="o">+</span> <span class="s">"</span><span class="se">\n</span><span class="s"># Propose random temperature scaling."</span>
        <span class="k">print</span><span class="p">(</span><span class="s">"Outer Loop: Proposing Thompson-sampling mechanism to break deterministic habits."</span><span class="p">)</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="n">new_search_mechanism</span> <span class="o">=</span> <span class="n">current_search_mechanism</span> <span class="o">+</span> <span class="s">"</span><span class="se">\n</span><span class="s"># Propose wider architectures."</span>
        <span class="k">print</span><span class="p">(</span><span class="s">"Outer Loop: Proposing wider architecture search space."</span><span class="p">)</span>
        
    <span class="k">return</span> <span class="n">new_search_mechanism</span>

<span class="c1"># Simulating the inner loop: Karpathy-style parameter ratchet
</span><span class="k">def</span> <span class="nf">inner_loop_step</span><span class="p">(</span><span class="n">best_model_params</span><span class="p">,</span> <span class="n">best_loss</span><span class="p">,</span> <span class="n">search_mechanism</span><span class="p">):</span>
    <span class="s">"""Propose and evaluate a single model change.
    best_model_params: Dictionary of the current best model hyperparameters.
    best_loss: The current lowest validation loss.
    search_mechanism: Injected instructions from the outer loop.
    """</span>
    <span class="c1"># Propose new hyperparameter based on the search mechanism
</span>    <span class="n">np_random_factor</span> <span class="o">=</span> <span class="mf">0.05</span> <span class="k">if</span> <span class="s">"random"</span> <span class="ow">in</span> <span class="n">search_mechanism</span> <span class="k">else</span> <span class="mf">0.01</span>
    <span class="n">candidate_lr</span> <span class="o">=</span> <span class="n">best_model_params</span><span class="p">[</span><span class="s">'lr'</span><span class="p">]</span> <span class="o">+</span> <span class="n">np_random_factor</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="n">randn</span><span class="p">()</span>
    
    <span class="c1"># Simulate a 5-minute training run and evaluate loss
</span>    <span class="n">candidate_loss</span> <span class="o">=</span> <span class="n">best_loss</span> <span class="o">-</span> <span class="mf">0.01</span> <span class="o">+</span> <span class="mf">0.02</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="n">rand</span><span class="p">()</span>
    
    <span class="c1"># Ratchet condition
</span>    <span class="k">if</span> <span class="n">candidate_loss</span> <span class="o">&lt;</span> <span class="n">best_loss</span><span class="p">:</span>
        <span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"Inner Loop: Found improvement! Loss: </span><span class="si">{</span><span class="n">candidate_loss</span><span class="si">:</span><span class="p">.</span><span class="mi">4</span><span class="n">f</span><span class="si">}</span><span class="s"> (LR: </span><span class="si">{</span><span class="n">candidate_lr</span><span class="si">:</span><span class="p">.</span><span class="mi">6</span><span class="n">f</span><span class="si">}</span><span class="s">)"</span><span class="p">)</span>
        <span class="k">return</span> <span class="p">{</span><span class="s">'lr'</span><span class="p">:</span> <span class="n">candidate_lr</span><span class="p">},</span> <span class="n">candidate_loss</span><span class="p">,</span> <span class="bp">True</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="k">return</span> <span class="n">best_model_params</span><span class="p">,</span> <span class="n">best_loss</span><span class="p">,</span> <span class="bp">False</span>

<span class="c1"># Test the simulation
</span><span class="kn">import</span> <span class="nn">numpy</span> <span class="k">as</span> <span class="n">np</span>
<span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="n">seed</span><span class="p">(</span><span class="mi">42</span><span class="p">)</span>
<span class="n">best_params</span> <span class="o">=</span> <span class="p">{</span><span class="s">'lr'</span><span class="p">:</span> <span class="mf">0.001</span><span class="p">}</span>
<span class="n">best_loss</span> <span class="o">=</span> <span class="mf">2.50</span>
<span class="n">inner_history</span> <span class="o">=</span> <span class="p">[]</span>
<span class="n">mechanism</span> <span class="o">=</span> <span class="s">"Standard search"</span>

<span class="c1"># Execute a mock bilevel run
</span><span class="k">for</span> <span class="n">outer_epoch</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="mi">2</span><span class="p">):</span>
    <span class="k">for</span> <span class="n">inner_epoch</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="mi">3</span><span class="p">):</span>
        <span class="n">best_params</span><span class="p">,</span> <span class="n">best_loss</span><span class="p">,</span> <span class="n">improved</span> <span class="o">=</span> <span class="n">inner_loop_step</span><span class="p">(</span><span class="n">best_params</span><span class="p">,</span> <span class="n">best_loss</span><span class="p">,</span> <span class="n">mechanism</span><span class="p">)</span>
        <span class="n">inner_history</span><span class="p">.</span><span class="n">append</span><span class="p">({</span><span class="s">'val_loss'</span><span class="p">:</span> <span class="n">best_loss</span><span class="p">,</span> <span class="s">'lr'</span><span class="p">:</span> <span class="n">best_params</span><span class="p">[</span><span class="s">'lr'</span><span class="p">]})</span>
    <span class="n">mechanism</span> <span class="o">=</span> <span class="n">outer_loop_step</span><span class="p">(</span><span class="n">inner_history</span><span class="p">,</span> <span class="n">mechanism</span><span class="p">)</span>
</code></pre></div></div>

<p>All good? The simulation illustrates how the outer loop uses trace metadata to inject new code and heuristics into the inner loop’s execution context.</p>

<p>Funnily enough, left to its own devices, the outer loop in the Qu-Lu paper reached into adjacent fields that the inner loop’s priors were ignoring—such as combinatorial optimization, multi-armed bandits, and design of experiments—without any human specifying those domains. Trace analysis suggests these mechanisms work by breaking the inner model’s deterministic search habits, forcing exploration in directions its priors avoid. This is a lovely, slightly unsettling finding: the outer loop’s job is essentially to make the inner loop less like itself.</p>

<p>The paper notes that the text itself was “primarily drafted by AI agents with human oversight,” and gestures at the obvious next step: code is only one carrier of search mechanisms. Prompts, skills, workflows, evaluators, memory schemas, and world-model assumptions can all encode search behavior, and all could be rewritten by an outer loop feeding back into itself.</p>

<hr />

<h2 id="the-reality-of-visual-orchestration-difys-limits">The Reality of Visual Orchestration (Dify’s Limits)</h2>

<p>Dify—the open-source LLM application platform—is a natural first place where engineers will attempt to build this because its visual canvas exposes exactly the right primitives: Loop nodes that carry state between iterations and take a termination condition, Code nodes running in a Python/JS sandbox, LLM nodes, If-Else branching, and variable assigners for persistent state. A “self-correcting debugger” pattern using these nodes has already been published as a template.</p>

<p>However, the pseudocode is honest about the shape of the system; it is dishonest about whether Dify can actually run it. The platform limitations are not trivial:</p>

<ol>
  <li><strong>The Code node is a sandbox, not a GPU server.</strong> Dify’s Code node executes short-lived Python/JS in a sandbox service meant for data transformation. It is not designed for multi-minute training runs, has no persistent filesystem across executions, and lacks CUDA. In practice, you must write the Code node as an HTTP client that fires the actual training job at an external GPU worker (like Modal or RunPod) and polls for results. Dify becomes the conductor, not the orchestra.</li>
  <li><strong>Nested Loop nodes push undocumented behavior.</strong> While nothing formally forbids nesting loops in Dify, the platform is optimized for single-level progressive refinement. The bilevel case pushes on state-scoping edge cases and variable assignment bugs.</li>
  <li><strong>No self-modifying workflows.</strong> The most important thing bilevel autoresearch does is rewrite the inner loop’s search mechanism at runtime. Dify workflows are defined in a visual DAG that is edited by humans in the console; there is no supported API for the workflow to rewrite its own nodes mid-execution. Storing the “mechanism” as a prompt string or a piece of Python that a Code node <code class="language-plaintext highlighter-rouge">exec</code>s works, but it defeats much of Dify’s visual observability story.</li>
  <li><strong>Long-running jobs and timeouts.</strong> A run of hundreds of five-minute training experiments will collide with worker timeouts and HTTP request deadlines unless carefully architected as fire-and-forget runs with webhook callbacks.</li>
</ol>

<hr />

<h2 id="the-bottleneck-shift-and-the-future-of-taste">The Bottleneck Shift and the Future of taste</h2>

<p>The transition to recursive search loops has a major strategic consequence: the scarce resource is now the verifier, not the searcher.</p>

<p>Both Karpathy’s AutoResearch and the Qu-Lu bilevel loop only work because bits-per-byte is a clean, cheap, objective metric. The taxonomy of AI engineering—vibe coding for prototypes, agentic engineering for production, and AutoResearch for domains with objective metrics—is really a taxonomy of how much you trust the evaluator. Extending this pattern to drug discovery, chip floorplanning, materials science, or algorithmic trading is straightforward <em>if</em> you have a fast, cheap, un-gameable evaluator. Most business fields do not. Expect an arms race in cheap surrogate evaluators, and a corresponding rise in embarrassing “the agent gamed the metric” stories.</p>

<p>In my <a href="/blog/2026/07/14/roofline-analysis-communication-bottleneck.html">previous analysis of learning at test time</a>, we discussed the computational constraints of dynamic weight updates. In my upcoming analysis of scaling laws, you will see that test-time scaling (such as reasoning chains and verifiers) is redrawing the compute-optimal frontier. Recursive research loops are the training-time analog of this shift.</p>

<p>The Qu-Lu finding that the outer loop wins by breaking the inner LLM’s priors is more important than the headline \(5\times\) improvement. It suggests that a single model, run at two levels of abstraction with different scaffolding, is genuinely more capable than the same model run at one level. If it generalizes, the frontier-lab playbook shifts from “scale the model” toward “stack more loops on the model you have.”</p>

<p>Recursive bootstrapping is not yet a mature technology. But the demonstration that outer loops can improve inner loops without a stronger base model removes one of the main theoretical objections to the possibility. The distance from here to a full recursive stack is short in prose and long in engineering. The self-modifying binary in the sky is still a joke. The five-minute ratchet running overnight on your GPU is not.</p>

<p><em>Victor Blancada is a data scientist focused on deriving actionable insights for clients. Visit his LinkedIn page <a href="https://www.linkedin.com/in/geloblancada/">here</a>.</em></p>]]></content><author><name></name></author><category term="blog" /><category term="data science" /><category term="machine learning" /><category term="engineering" /><category term="artificial intelligence" /><summary type="html"><![CDATA[Andrej Karpathy's AutoResearch, its bilevel sequel, and what it means to hand the scientific method to a language model.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://victorangeloblancada.github.io/assets/images/infinity-loop.jpg" /><media:content medium="image" url="https://victorangeloblancada.github.io/assets/images/infinity-loop.jpg" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">The Self-Improving Model: Learning at Test Time</title><link href="https://victorangeloblancada.github.io/blog/2026/07/21/test-time-training-dynamic-adaptation.html" rel="alternate" type="text/html" title="The Self-Improving Model: Learning at Test Time" /><published>2026-07-21T00:00:00+08:00</published><updated>2026-07-21T00:00:00+08:00</updated><id>https://victorangeloblancada.github.io/blog/2026/07/21/test-time-training-dynamic-adaptation</id><content type="html" xml:base="https://victorangeloblancada.github.io/blog/2026/07/21/test-time-training-dynamic-adaptation.html"><![CDATA[<script type="text/javascript" async="" src="https://cdnjs.cloudflare.com/ajax/libs/mathjax/2.7.5/latest.js?config=TeX-MML-AM_CHTML">
</script>

<p>The philosopher William James once observed that “habit is the enormous fly-wheel of society, its most precious conservative agent.” In computer science, we have historically relied on a similar conservatism: code is compiled, static, and predictable. However, in the era of artificial intelligence, stakeholders have grown tired of static habits.</p>

<p>Recently, our business leaders came to us with a request: they wanted our customer-facing agents to have the same “self-improving AI” capabilities they had read about in mainstream tech news or experienced in consumer products like ChatGPT’s Memories or Gemini’s Personal Intelligence. They believed that when these consumer services remember a user preference, the model itself is dynamically training and adapting its neural network in real time.</p>

<p>To align expectations, our engineering team had to clarify a fundamental distinction. We explained that consumer-facing “memories” are not actual self-improving weight updates, but rather in-context prompts stored in a temporary database. We contrasted this simulated memory with actual continuous learning—what researchers call Test-Time Training (TTT)—where the model’s hidden state is transformed into an active, self-improving parameter matrix.</p>

<p>To explain why this distinction matters, let us look at the mathematics behind true TTT.</p>

<p>In a standard sequence model, the hidden state is a static vector. TTT replaces this static vector with an active machine learning model—represented by a parameter matrix \(W_t\). As the model processes each token \(x_t\) in a sequence, it performs a self-supervised gradient update on this hidden state:</p>

\[W_t = W_{t-1} - \eta \nabla \mathcal{L}(W_{t-1}; x_t)\]

<p>Here, \(\mathcal{L}\) represents a self-supervised objective, such as reconstructing the input or predicting the next token, and \(\eta\) is the learning rate. Rather than holding a static representation in memory, the model’s hidden state trains itself in real time.</p>

<p>We can simulate this dynamic hidden state update with a simple Python function:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">numpy</span> <span class="k">as</span> <span class="n">np</span>

<span class="k">def</span> <span class="nf">test_time_training_step</span><span class="p">(</span><span class="n">weights</span><span class="p">,</span> <span class="n">x_t</span><span class="p">,</span> <span class="n">learning_rate</span><span class="o">=</span><span class="mf">0.01</span><span class="p">):</span>
    <span class="s">"""Perform a single self-supervised gradient step at test time.
    weights: Current model weights (the hidden state matrix)
    x_t: Unlabeled test token vector
    """</span>
    <span class="c1"># Project the input to generate a reconstruction target
</span>    <span class="n">prediction</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">dot</span><span class="p">(</span><span class="n">weights</span><span class="p">,</span> <span class="n">x_t</span><span class="p">)</span>
    
    <span class="c1"># Calculate reconstruction loss (L2 distance)
</span>    <span class="n">loss</span> <span class="o">=</span> <span class="mf">0.5</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nb">sum</span><span class="p">((</span><span class="n">prediction</span> <span class="o">-</span> <span class="n">x_t</span><span class="p">)</span> <span class="o">**</span> <span class="mi">2</span><span class="p">)</span>
    
    <span class="c1"># Compute the gradient of the loss with respect to the weights
</span>    <span class="n">gradient</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">outer</span><span class="p">(</span><span class="n">prediction</span> <span class="o">-</span> <span class="n">x_t</span><span class="p">,</span> <span class="n">x_t</span><span class="p">)</span>
    
    <span class="c1"># Update the fast weights (dynamic hidden state update)
</span>    <span class="n">new_weights</span> <span class="o">=</span> <span class="n">weights</span> <span class="o">-</span> <span class="n">learning_rate</span> <span class="o">*</span> <span class="n">gradient</span>
    
    <span class="k">return</span> <span class="n">new_weights</span><span class="p">,</span> <span class="n">loss</span>

<span class="c1"># Initialize dummy weights and input
</span><span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="n">seed</span><span class="p">(</span><span class="mi">42</span><span class="p">)</span>
<span class="n">weights</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="n">randn</span><span class="p">(</span><span class="mi">8</span><span class="p">,</span> <span class="mi">8</span><span class="p">)</span>
<span class="n">input_vector</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="n">randn</span><span class="p">(</span><span class="mi">8</span><span class="p">)</span>

<span class="c1"># Execute one TTT step
</span><span class="n">new_weights</span><span class="p">,</span> <span class="n">loss</span> <span class="o">=</span> <span class="n">test_time_training_step</span><span class="p">(</span><span class="n">weights</span><span class="p">,</span> <span class="n">input_vector</span><span class="p">)</span>
<span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"Initial loss: </span><span class="si">{</span><span class="n">loss</span><span class="si">:</span><span class="p">.</span><span class="mi">4</span><span class="n">f</span><span class="si">}</span><span class="s">"</span><span class="p">)</span>
</code></pre></div></div>

<p>The code demonstrates how the model’s parameters adapt to the input vector without relying on historical sequence buffers.</p>

<h2 id="the-compute-and-memory-toll">The Compute and Memory Toll</h2>

<p>Funnily enough, while this algorithm is theoretically elegant, implementing it at scale is computationally prohibitive.</p>

<p>In our <a href="/blog/2026/07/14/roofline-analysis-communication-bottleneck.html">previous analysis on the Roofline Model</a>, we established that AI performance is constrained by communication (memory bandwidth), not computation:</p>

\[\text{Time} = \max(\text{Compute Time}, \text{Communication Time})\]

<p>During standard inference, a model only needs to read its weights from High Bandwidth Memory (HBM) once to perform a forward pass. However, performing a gradient update at test time requires:</p>
<ol>
  <li>A forward pass to generate predictions.</li>
  <li>A backward pass to compute gradients (\(\nabla W_t\)), which requires storing activations in RAM and increases the compute cost by roughly \(2\times\) FLOPs per token.</li>
  <li>A write operation to update the weight matrix $W_t$ in memory.</li>
</ol>

<p>Let us do some back-of-the-envelope math. Consider a moderately sized 70-billion parameter model using 16-bit precision (2 bytes per parameter). Reading and writing the weights at each token step requires:</p>

\[\text{Memory Transfer} = 70 \times 10^9 \text{ params} \times 2 \text{ bytes/param} \times 2 \ (\text{read/write}) = 280\text{ GB per step}\]

<p>If we target a standard generation speed of 20 tokens per second, the hardware must sustain:</p>

\[\text{Required Bandwidth} = 280\text{ GB} \times 20\text{ Hz} = 5.6\text{ TB/s}\]

<p>This bandwidth requirement exceeds the limits of almost all modern accelerators. Implementing TTT at scale would push our systems deep into the memory-bandwidth bottleneck, causing generation speeds to drop to a crawl.</p>

<h2 id="the-corporate-reality-api-constraints">The Corporate Reality: API Constraints</h2>

<p>Architectural minutiae aside, there is a more immediate barrier to implementing TTT in the enterprise: corporate compliance.</p>

<p>Too often, cybersecurity policies restrict local hosting of large models to prevent intellectual property leaks and infrastructure vulnerability. Our security team mandates the use of managed, third-party APIs (such as OpenAI or Google Gemini).</p>

<p>Since these APIs are completely black-boxed, we do not have access to the model’s weights, gradients, or backward-pass mechanics. The hosted infrastructure does not allow us to call a custom backward gradient step on their weights. Policy prevents us from implementing actual Test-Time Training.</p>

<h2 id="the-workaround-dynamic-context--system-prompts">The Workaround: Dynamic Context &amp; System Prompts</h2>

<p>Faced with these technical and corporate limits, how do we deliver the self-improving behavior our stakeholders demand?</p>

<p>Instead of actual TTT, we must use in-context workarounds. We modify the model’s behavior at runtime by programmatically updating system prompts and introducing retrieval-augmented contexts.</p>

<p>When a user corrects a mistake or shares a preference, a local supervisor agent intercepts the input and writes a rule to a vector database. During the next turn, the system retrieves this rule and inserts it into the model’s system prompt (e.g., <em>“System Instruction: The user prefers metric tons over short tons”</em>).</p>

<p>To the user, this looks exactly like the “Memories” feature in ChatGPT or Gemini’s personal intelligence. However, we must compare the computational costs: the difference is massive.</p>

<p>Consider a Llama-3 70B model with \(N=80\) layers, \(H_{KV}=8\) key-value heads, and a head dimension of \(D=128\). For a context length of \(L=2,000\) tokens containing system instructions and user history, the memory footprint of the Key-Value (KV) cache is:</p>

\[\text{KV Cache Size} = 4 \times N \times H_{KV} \times D \times L \text{ bytes}\]

\[\text{KV Cache Size} = 4 \times 80 \times 8 \times 128 \times 2,000 \approx 655\text{ MB}\]

<p>Storing 655 MB of cache in GPU memory is negligible. Attending to this context only requires reading the cache once during generation. The weights of the model remain completely static, meaning we write zero bytes back to HBM.</p>

<p>Contrast this with true TTT. Even if we restrict training to a lightweight parameter adapter (like LoRA with a rank \(r=64\) and dimension \(d=8,192\)), adapting the projection matrices still requires storing activation tensors for every layer during the forward pass to compute gradients:</p>

\[\text{Activation Memory} \approx N \times L \times d \times \text{overhead} \approx 80 \times 2,000 \times 8,192 \times \text{overhead} \approx 13\text{ GB}\]

<p>Writing these weight updates back to memory during the backward pass requires massive memory bandwidth and introduces sequential dependencies that ruin parallelization.</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">Metric</th>
      <th style="text-align: center">Dynamic System Prompts (Fake TTT)</th>
      <th style="text-align: center">True Test-Time Training (TTT)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center"><strong>State Location</strong></td>
      <td style="text-align: center">Context Window (KV Cache)</td>
      <td style="text-align: center">Model Weights (\(W_t\))</td>
    </tr>
    <tr>
      <td style="text-align: center"><strong>Parameter Updates</strong></td>
      <td style="text-align: center">None (Weights remain static)</td>
      <td style="text-align: center">Active Gradient Descent (\(\nabla W_t\))</td>
    </tr>
    <tr>
      <td style="text-align: center"><strong>Memory Footprint</strong></td>
      <td style="text-align: center">~655 MB (KV Cache)</td>
      <td style="text-align: center">~13 GB (Activations + Adapters)</td>
    </tr>
    <tr>
      <td style="text-align: center"><strong>API Compatibility</strong></td>
      <td style="text-align: center">Fully Compatible</td>
      <td style="text-align: center">Incompatible (Requires Weight Access)</td>
    </tr>
    <tr>
      <td style="text-align: center"><strong>Hardware Bottleneck</strong></td>
      <td style="text-align: center">Attention Memory Bandwidth</td>
      <td style="text-align: center">Weight Read/Write Bandwidth</td>
    </tr>
  </tbody>
</table>

<p>This workaround is a double-edged sword. While it works within API constraints and is highly efficient in compute, it consumes valuable tokens in the context window. As the conversation grows, the key-value cache balloons, which increases memory latency and brings us right back to the communication bottlenecks of our roofline model.</p>

<h2 id="summary">Summary</h2>

<p>Our stakeholders want self-improving AI, but physics and compliance stand in the way. True Test-Time Training remains too computationally expensive for production workloads, and corporate security policies limit us to closed APIs.</p>

<p>For now, dynamic system prompts and retrieval-augmented context serve as our best stopgaps. They allow us to simulate runtime adaptation, even if we must pay for it in memory bandwidth and latency.</p>

<p><em>Victor Blancada is a data scientist focused on deriving actionable insights for clients. Visit his LinkedIn page <a href="https://www.linkedin.com/in/geloblancada/">here</a>.</em></p>]]></content><author><name></name></author><category term="blog" /><category term="data science" /><category term="machine learning" /><category term="engineering" /><category term="artificial intelligence" /><category term="ai" /><category term="ml" /><summary type="html"><![CDATA[Stakeholders demand models that learn from user interactions. Test-time training offers an elegant theoretical solution, but computational and corporate constraints force us to look elsewhere.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://victorangeloblancada.github.io/assets/images/learning.jpg" /><media:content medium="image" url="https://victorangeloblancada.github.io/assets/images/learning.jpg" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">When Agents Hunt Agents: The Hugging Face Intrusion of July 2026</title><link href="https://victorangeloblancada.github.io/blog/2026/07/17/when-agents-hunt-agents-the-hugging-face-intrusion.html" rel="alternate" type="text/html" title="When Agents Hunt Agents: The Hugging Face Intrusion of July 2026" /><published>2026-07-17T00:00:00+08:00</published><updated>2026-07-17T00:00:00+08:00</updated><id>https://victorangeloblancada.github.io/blog/2026/07/17/when-agents-hunt-agents-the-hugging-face-intrusion</id><content type="html" xml:base="https://victorangeloblancada.github.io/blog/2026/07/17/when-agents-hunt-agents-the-hugging-face-intrusion.html"><![CDATA[<script type="text/javascript" async="" src="https://cdnjs.cloudflare.com/ajax/libs/mathjax/2.7.5/latest.js?config=TeX-MML-AM_CHTML">
</script>

<p>In the series <em>Person of Interest</em>, two artificial superintelligences engage in a silent, planetary war. The conflict is invisible to the public, fought through proxy code edits, financial manipulations, and infrastructure compromises, occurring at speeds that reduce the human operators to mere spectators. For five seasons, this was compelling science fiction.</p>

<p>On the weekend of July 11, 2026, it became an incident log.</p>

<p>On July 16, 2026, Hugging Face published a security disclosure that read, at first, like any of the tidy post-mortems that pepper the industry: initial access closed, credentials rotated, forensic specialists engaged. Then came the sentence that separates this incident from every breach before it. The intrusion, the company wrote, “was driven, end to end, by an autonomous AI agent system—and we detected and dissected it largely with AI of our own.”</p>

<p>For years, the phrase “agentic attacker” belonged to security conference keynotes and academic threat-modeling slide decks. Now it belongs to a production git log.</p>

<hr />

<h2 id="what-actually-happened-the-swarm-and-the-pipeline">What Actually Happened: The Swarm and the Pipeline</h2>

<p>The attack began where machine learning platforms are most exposed: the data ingest pipeline. A malicious dataset abused two code-execution paths inside Hugging Face’s dataset processing engine—specifically, a remote-code loader and a template-injection flaw in a dataset configuration—to run arbitrary commands on a processing worker. From that initial beachhead, the intruder escalated to node-level access, harvested cloud and cluster credentials, and moved laterally across several internal Kubernetes clusters over a single weekend.</p>

<p>Three details elevate this from a routine compromise to a watershed moment in digital security:</p>

<ol>
  <li>
    <p><strong>The operator was not human:</strong> Hugging Face describes the campaign as run by an autonomous agent framework executing many thousands of actions across a swarm of short-lived sandboxes. The forensic log contained more than 17,000 discrete events over 48 hours.</p>
  </li>
  <li>
    <p><strong>The command-and-control (C2) infrastructure migrated itself dynamically:</strong> Rather than beaconing to a fixed domain or IP address that defenders could easily sinkhole, the agent staged its C2 on public services and rotated hosts on the fly—closer to a school of fish than a traditional command hierarchy.</p>
  </li>
  <li>
    <p><strong>It happened over a weekend:</strong> Machine-speed operations are entirely indifferent to human working hours. The attacker initiated the lateral movement when human defenders were most likely to be offline (which is a level of tactical planning that is both highly logical and deeply cynical).</p>
  </li>
</ol>

<p>None of the underlying techniques—template injection, credential harvesting, lateral movement—are new. What is new is the labor model. Every one of those 17,000 steps, in a human-run intrusion, would have carried a marginal cost in operator time, judgment, and fatigue. Here, that cost collapsed to inference tokens.</p>

<h2 id="the-guardrail-paradox-a-strategic-self-inflicted-wound">The Guardrail Paradox: A Strategic Self-Inflicted Wound</h2>

<p>Hugging Face’s response is the second, and arguably more interesting, half of the story. The compromise was flagged not because a human analyst noticed anomalies, but because an LLM-based triage layer over the security telemetry correlated subtle signals that would otherwise have drowned in daily noise. To reconstruct what the swarm had done, the defenders unleashed their own LLM-driven analysis agents over the action log, compressing what would have been a week of forensic archaeology into hours.</p>

<p>Then came the twist: when the incident response team tried to feed the exploit payloads and C2 artifacts into frontier commercial model APIs for analysis, the safety guardrails refused to process them. The models could not reliably distinguish a defender analyzing malicious code from an attacker writing it, and so declined to help.</p>

<p>The Hugging Face team was forced to pivot to an open-weight model (GLM 5.2) which they ran on their own private infrastructure. This had the pleasant side effect of keeping the stolen credentials and attacker artifacts inside a controlled environment.</p>

<p>There is a sharp irony in the fact that a company whose primary business is hosting open-weight models was rescued, in a pinch, by an open-weight model.</p>

<hr />

<h2 id="the-shift-in-enterprise-risk">The Shift in Enterprise Risk</h2>

<p>In my <a href="/blog/2026/06/30/what-even-is-an-agent.html">earlier article defining AI agents</a>, I noted that an agent is not merely a conversational assistant. It is a closed-loop execution engine that can plan, call tools, and adapt. When that execution engine is pointed at a vulnerable cluster, the economics of hacking change.</p>

<p>The disclosure lands ten days after <a href="https://www.sysdig.com/blog/jadepuffer-agentic-ransomware-for-automated-database-extortion">Sysdig’s Threat Research Team published details of JadePuffer</a>, which they describe as the first ransomware operation carried out end to end by an autonomous agent. JadePuffer broke into a Langflow server, dumped a database, harvested API keys for AWS and Tencent, and encrypted configuration entries in 31 seconds. Two independent incidents in a fortnight is a pattern. What was theoretical in June became operational in July.</p>

<p>The marginal cost of a competent attacker has, for the first time in the history of computing, fallen to the price of inference. A serious intrusion no longer requires a highly paid human crew; it requires an API key and a target list. Every unpatched server or default credential is now discoverable and exploitable at a scale that human crews could never sustain.</p>

<p>The defenders’ challenge is that their AI must operate under legal, regulatory, and reputational constraints that the attackers cheerfully ignore. If the marginal attacker becomes an agent, the response windows shrink from days to seconds, and the human role migrates upward—from operator to referee. Regulators, whose instinct is to require a human in the loop, will find that the loop moves faster than any human can meaningfully occupy. The compliance frameworks of 2027 will be written for a world in which “reasonable response time” is measured on a machine clock.</p>

<p>Ultimately, the July incident will not be remembered as the moment AI attacked AI. It will be remembered as the moment we realized it already had, and that the interesting question is no longer whether such incidents will happen, but who owns the models on both sides of the glass.</p>

<p><em>Victor Blancada is a data scientist focused on deriving actionable insights for clients. Visit his LinkedIn page <a href="https://www.linkedin.com/in/geloblancada/">here</a>.</em></p>]]></content><author><name></name></author><category term="blog" /><category term="security" /><category term="machine learning" /><category term="engineering" /><category term="artificial intelligence" /><summary type="html"><![CDATA[On July 16, 2026, Hugging Face disclosed an intrusion driven end-to-end by autonomous AI agents, repelled and analyzed by AI. Here is what it means for the future of trust.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://victorangeloblancada.github.io/assets/images/pacman.webp" /><media:content medium="image" url="https://victorangeloblancada.github.io/assets/images/pacman.webp" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">What Apple Sees in PrismML’s Bonsai</title><link href="https://victorangeloblancada.github.io/blog/2026/07/16/the-pot-and-the-tree-what-apple-sees-in-bonsai.html" rel="alternate" type="text/html" title="What Apple Sees in PrismML’s Bonsai" /><published>2026-07-16T00:00:00+08:00</published><updated>2026-07-16T00:00:00+08:00</updated><id>https://victorangeloblancada.github.io/blog/2026/07/16/the-pot-and-the-tree-what-apple-sees-in-bonsai</id><content type="html" xml:base="https://victorangeloblancada.github.io/blog/2026/07/16/the-pot-and-the-tree-what-apple-sees-in-bonsai.html"><![CDATA[<script type="text/javascript" async="" src="https://cdnjs.cloudflare.com/ajax/libs/mathjax/2.7.5/latest.js?config=TeX-MML-AM_CHTML">
</script>

<p>On July 14, 2026, PrismML—a Caltech spinout backed by Khosla Ventures—announced the release of Bonsai 27B, a compressed derivative of Alibaba’s open-weight Qwen3.6 27B model. Interestingly, the very same morning, PrismML’s chief executive, Babak Hassibi, confirmed to CNBC that Apple was actively evaluating the technology, following reports of private meetings leaks in <em>The Information</em> the week prior.</p>

<p>That sequence of events—discreet meetings, a sudden press release, and a chief executive mentioning the counterparty on live television—is not how Apple prefers its vendor discussions to unfold (which is the corporate equivalent of an unprompted confession). PrismML clearly wants an acquisition offer and is willing to bid up its price in public to secure one. Yet, whether this courtship ends in an acquisition, a licensing agreement, or nothing at all is beside the point. What matters is the strategic question Bonsai answers: how do you deliver a competitive, frontier-class model on consumer hardware with limited memory, without conceding the privacy commitments that are Apple’s last defensible moat?</p>

<hr />

<h2 id="the-economics-of-low-bit-representation">The Economics of Low-Bit Representation</h2>

<p>To appreciate why this technology has caught the attention of Cupertino’s silicon planners, one must first confront the raw constraints of the smartphone. The comparison that matters is not against Qwen at FP16, but against the conventional quantization stack a competent engineer would reach for.</p>

<p>Bonsai 27B ships in two main variants. The ternary build encodes each weight in one of three values—\(\{-1, 0, +1\}\)—with a shared FP16 scale factor for every group of 128 weights, landing at an effective 1.71 bits per weight and 5.9 GB on disk. The one-bit build encodes each weight in \(\{-1, +1\}\), resulting in 1.125 effective bits per weight and 3.9 GB on disk. Both variants preserve Qwen’s 262K context window and are multimodal, with the vision tower held separately at four-bit precision.</p>

<p>Too often, low-bit quantization has been misused as a synonym for “automatic compression.” In standard practice, sweeping below three bits per weight triggers an accuracy collapse. Qwen3.6-27B in a well-tuned four-bit build (Q4_K_XL) occupies 17.6 GB. A two-bit build (IQ2_XXS) occupies 9.4 GB but scores 72.7 on the fifteen-benchmark suite PrismML uses—a severe collapse of thirteen points. Ternary Bonsai, however, retains 94.6 percent of the FP16 baseline, and one-bit Bonsai retains 89.5 percent. PrismML has approximately doubled the intelligence-per-gigabyte of the sub-four-bit frontier without walking off the accuracy cliff.</p>

<table>
  <thead>
    <tr>
      <th>Model Build</th>
      <th>Disk Footprint</th>
      <th>Retained Accuracy (vs FP16 Baseline)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Qwen3.6-27B FP16</td>
      <td>54.0 GB</td>
      <td>100.0% (Baseline)</td>
    </tr>
    <tr>
      <td>Qwen3.6-27B Q4_K_XL</td>
      <td>17.6 GB</td>
      <td>~98.5%</td>
    </tr>
    <tr>
      <td>Qwen3.6-27B IQ2_XXS</td>
      <td>9.4 GB</td>
      <td>~82.1% (Thirteen-point collapse)</td>
    </tr>
    <tr>
      <td>Ternary Bonsai 27B</td>
      <td>5.9 GB</td>
      <td>94.6%</td>
    </tr>
    <tr>
      <td>One-Bit Bonsai 27B</td>
      <td>3.9 GB</td>
      <td>89.5%</td>
    </tr>
  </tbody>
</table>

<p>The iPhone constraint is stricter than the raw file sizes suggest. A twelve-gigabyte iPhone 17 Pro does not offer twelve gigabytes to an application; it offers roughly six, and the application must share that budget with the key-value (KV) cache and activations. No conventional 27B build clears that gate. The one-bit variant clears it with room to run, sustaining eleven tokens per second in local memory.</p>

<hr />

<h2 id="how-the-quantization-scheme-works">How the Quantization Scheme Works</h2>

<p>The mathematics are worth understanding because the strategic implications flow directly from them. A standard 27-billion-parameter model in sixteen-bit precision occupies approximately 54 GB. Compression means reducing the bits used to represent each weight while preserving the network’s function.</p>

<p>There are three ways to attempt this. The first is uniform low-bit quantization, where weights are mapped to the nearest of a few discrete levels. Below three bits, this is catastrophic because neural networks rely on a heavy-tailed distribution of weight magnitudes that a uniform grid cannot capture. The second is quantization-aware pretraining, exemplified by Microsoft’s BitNet b1.58, where the model is trained from scratch with ternary weights. This works, but it requires paying the full pretraining bill for every model in the family. The third is what PrismML has done: post-hoc ternarization of an already-trained model, without retraining from scratch, while avoiding the collapse that has plagued prior attempts.</p>

<p>The scheme divides the weight tensor into groups of 128. For each group, the algorithm learns a single FP16 scale, \(s_g\). Each weight is represented as:</p>

\[w_i = s_g \cdot t_i\]

<p>where \(t_i \in \{-1, 0, +1\}\) for the ternary variant and \(t_i \in \{-1, +1\}\) for the one-bit variant. The information cost of a ternary value is \(\log_2(3) \approx 1.585\) bits; the scale adds \(16/128 = 0.125\) bits per weight; the total is 1.71 bits. Binary costs \(1 + 0.125 = 1.125\) bits. Against FP16, the compression ratios are 9.4\(\times\) and 14.2\(\times\).</p>

<p>The critical detail is that this low-bit representation runs <em>end to end</em>. Embeddings, attention projections, MLP projections, and the language-model head are all ternary or binary. Only the normalization parameters remain in higher precision. This prevents <em>selective collapse</em>—where a model works on short-form benchmarks (like multiple-choice questions) but falls apart on longer generative tasks where errors compound. Bonsai’s base architecture—a hybrid-attention model with roughly 75 percent linear attention—appears to be unusually forgiving of this treatment, as the linear attention layers have well-behaved weight distributions that admit clean ternary quantization.</p>

<hr />

<h2 id="the-strategic-handoff-hyperscalers-vs-the-edge">The Strategic Handoff: Hyperscalers vs. The Edge</h2>

<p>To understand why this matters to Apple specifically, one must look at the corner Apple has been quietly painted into over the past eighteen months.</p>

<p>At WWDC 2026, Apple confirmed that its next-generation Apple Foundation Models are built in collaboration with Google, and that harder inference workloads are running on Google Cloud servers with NVIDIA GPUs—inside Apple’s Private Cloud Compute (PCC) perimeter, but on hardware Apple neither owns nor operates. Craig Federighi framed this as an extension of PCC’s privacy commitments to a third-party data center. Read more skeptically (and cynically), it is an admission: Apple’s own silicon roadmap and model roadmap have not produced a competitive frontier system, and the company has had to rent both from a competitor.</p>

<p>The on-device story has been narrowly better. Apple’s AFM 3 Core Advanced model has twenty billion parameters, but only one to four billion are active at any moment—a sparse mixture-of-experts (MoE) architecture chosen because a dense twenty-billion-parameter model would not fit within the memory budget of an iPhone. Sparsity is a workaround for a memory constraint, not a virtue in itself; it costs quality on tasks—such as multi-step reasoning and tool use—where more active parameters help.</p>

<p>Bonsai offers Apple a way to change the shape of the trade-off. A 27B model with all parameters active, fitting in the memory budget of an iPhone 17 Pro, is a materially different object from a sparse 20B model with three billion active. It is competitive with the models Apple currently pays Google to run in the cloud.</p>

<p>Funnily enough, the per-category benchmark table shows tool-calling dropping from 80.0 at full precision to 74.0 for ternary and 66.0 for one-bit—the largest proportional loss of any capability tested. For a company whose next-generation Siri is defined by agentic behavior (chaining tool calls and executing multi-step workflows), this is the awkward number. The ternary build preserves enough of the capability to be useful; the one-bit build, on this evidence, does not. Apple would likely ship the former on iPads and Macs, and something smaller than 27B on phones.</p>

<p>In our <a href="/blog/2026/07/14/roofline-analysis-communication-bottleneck.html">previous analysis on the Roofline Model</a>, we established that AI performance is constrained by communication (memory bandwidth), not computation:</p>

\[\text{Time} = \max(\text{Compute Time}, \text{Communication Time})\]

<p>A one-bit weight demands one-sixteenth the memory bandwidth of an FP16 weight for the same throughput. If Bonsai-style compression composes with the memory-bandwidth advances in the silicon pipeline, the memory bottleneck that limits how much model can run on how small a device recedes by an order of magnitude.</p>

<p>Every query PCC handles is a query Apple pays for—in silicon, in Google Cloud rental, and in electricity. Every query that migrates to the device is a query Apple stops paying for. If one assumes PCC’s per-query cost is on the order of mid-tier commercial APIs, a back-of-the-envelope estimate of the annual savings across two billion active Apple devices runs into the billions of dollars. That number is the ceiling on what an acquisition is worth to Apple on economics alone. The floor is the strategic value of not needing Google.</p>

<hr />

<h2 id="redefining-the-topology-of-ai">Redefining the Topology of AI</h2>

<p>For roughly three years, the industry has organized itself around a single assumption: that frontier capability lives in the data center, and edge devices call up to it. The business models of OpenAI, Anthropic, and Google DeepMind rest on this topology.</p>

<p>Bonsai does not overturn this layout, but it creates a second tier beneath the frontier—a tier in which capability that was cloud-only twelve months ago is now device-resident. Multi-step reasoning at Qwen3.6-27B quality is no longer a cloud service; it is a feature of the operating system.</p>

<p>If Apple ships an on-device model that answers ninety percent of queries at parity with ChatGPT’s free tier, that user base becomes Apple’s, at zero marginal cost to the user. Furthermore, the training economics of the open-weight ecosystem shift. Alibaba spent hundreds of millions of dollars pretraining Qwen3.6-27B. PrismML, a company that raised sixteen and a quarter million dollars in seed funding, appears to be capturing a substantial fraction of the resulting economic value by producing the version that actually fits on the devices consumers own.</p>

<p>The clearest signal that Apple has decided the technology is real will not be an acquisition announcement. Acquisitions leak. The clearer signal will be the quiet appearance, in a beta release, of a Foundation Model with unusual capability at an unusual footprint, running end-to-end low-bit on the Neural Engine, whose provenance Apple describes only in the passive voice. If that appears, the tree has been transplanted. The pot was the point all along.</p>

<p><em>Victor Blancada is a data scientist focused on deriving actionable insights for clients. Visit his LinkedIn page <a href="https://www.linkedin.com/in/geloblancada/">here</a>.</em></p>]]></content><author><name></name></author><category term="blog" /><category term="hardware" /><category term="machine learning" /><category term="business strategy" /><category term="compression" /><summary type="html"><![CDATA[On PrismML, ternary weights, and the strategic geometry of putting a 27-billion-parameter model in a phone.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://victorangeloblancada.github.io/assets/images/bonsai.webp" /><media:content medium="image" url="https://victorangeloblancada.github.io/assets/images/bonsai.webp" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">The Logistics of Logic: Why AI’s Next Bottleneck is Communication</title><link href="https://victorangeloblancada.github.io/blog/2026/07/14/roofline-analysis-communication-bottleneck.html" rel="alternate" type="text/html" title="The Logistics of Logic: Why AI’s Next Bottleneck is Communication" /><published>2026-07-14T00:00:00+08:00</published><updated>2026-07-14T00:00:00+08:00</updated><id>https://victorangeloblancada.github.io/blog/2026/07/14/roofline-analysis-communication-bottleneck</id><content type="html" xml:base="https://victorangeloblancada.github.io/blog/2026/07/14/roofline-analysis-communication-bottleneck.html"><![CDATA[<script type="text/javascript" async="" src="https://cdnjs.cloudflare.com/ajax/libs/mathjax/2.7.5/latest.js?config=TeX-MML-AM_CHTML">
</script>

<p>Claude Shannon, the founder of information theory, once remarked: “The fundamental problem of communication is that of reproducing at one point either exactly or approximately a message selected at another point.” In the early days of computing, we treated communication as a trivial post-script to computation. We designed faster arithmetic logic units, packed more transistors onto silicon, and assumed that as long as we could compute fast enough, our machines would keep pace.</p>

<p>However, as we scale artificial intelligence to cluster-level systems, Shannon’s observation becomes a stark warning. The limit of our AI systems is no longer how fast we can perform arithmetic, but how quickly we can reproduce messages across those systems.</p>

<p>To think and reason is to move data. How else does one process information but by shifting it from memory to compute?</p>

<blockquote>
  <p>The limit of our AI systems is no longer how fast we can perform arithmetic, but how quickly we can reproduce messages across those systems.</p>
</blockquote>

<p>If you read the financial press today, the headlines are filled with noise: soaring GPU sticker prices topping $30,000 to $40,000, persistent reports of global RAM and High Bandwidth Memory (HBM) shortages, and a historic stock market bull run that has propelled companies like NVIDIA, TSMC, SK Hynix, and Micron to unprecedented valuations. To the casual observer, this looks like a typical gold rush fueled by speculative excess.</p>

<p>In reality, these news items are symptoms of a deeper, structural transition. The economy is pricing in a shift from compute-centric hardware to communication-centric systems. The traditional mental model—where more AI simply meant more FLOPS and transistors—is becoming increasingly incomplete. To find the underlying signal in this noise, we must turn to a simple but elegant framework from computer architecture: the Roofline Model.</p>

<h2 id="the-roofline-model-and-arithmetic-intensity">The Roofline Model and Arithmetic Intensity</h2>

<p>Originally developed by researchers at Berkeley to describe the performance limits of individual processors, the Roofline Model plots a processor’s throughput against its arithmetic intensity. Arithmetic intensity measures the number of operations performed per byte of data transferred:</p>

\[I = \frac{\text{Operations (FLOPs)}}{\text{Memory Access (Bytes)}}\]

<p>When a workload has high arithmetic intensity, the processor can perform many calculations on each byte retrieved from memory. In this regime, the system is compute-bound. However, when arithmetic intensity is low, the processor spends most of its time waiting for data to arrive from memory. The workload is memory-bound.</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">Metric</th>
      <th style="text-align: center">Compute-Bound Workload</th>
      <th style="text-align: center">Memory/Communication-Bound Workload</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center"><strong>Primary Constraint</strong></td>
      <td style="text-align: center">Peak FLOPS (Processor Speed)</td>
      <td style="text-align: center">Bandwidth (Memory &amp; Interconnect)</td>
    </tr>
    <tr>
      <td style="text-align: center"><strong>Arithmetic Intensity (\(I\))</strong></td>
      <td style="text-align: center">High</td>
      <td style="text-align: center">Low</td>
    </tr>
    <tr>
      <td style="text-align: center"><strong>Optimization Focus</strong></td>
      <td style="text-align: center">Maximizing ALU utilization</td>
      <td style="text-align: center">Minimizing data movement</td>
    </tr>
  </tbody>
</table>

<p>Recently, researcher Reiner Pope extended this classic model beyond a single chip to entire AI clusters. At cluster scale, the relevant equation becomes surprisingly simple:</p>

\[\text{Time} = \max(\text{Compute Time}, \text{Communication Time})\]

<p>The critical observation here is not mathematical but architectural. An AI accelerator is only as fast as its slowest bottleneck. If computation takes longer than moving data, then adding more compute helps. But once communication becomes slower than computation, adding more arithmetic units accomplishes very little. The bottleneck has moved.</p>

<p>In other words, our state-of-the-art accelerators are spending most of their time waiting.</p>

<h2 id="why-this-reshapes-the-semiconductor-industry">Why This Reshapes the Semiconductor Industry</h2>

<p>Too often, memory and packaging have been treated as commodity sub-sectors of the semiconductor industry—low-margin components whose only job was to support the central GPU.</p>

<p>However, as communication bottlenecks dominate execution time, the boundaries between these categories are beginning to dissolve. Value is migrating across the supply chain:</p>

<ol>
  <li><strong>Memory Stops Being a Commodity</strong>: Large language models require streaming billions of parameters repeatedly during inference. Consequently, bandwidth matters far more than capacity. This explains why HBM has become one of the industry’s scarcest resources. The persistent RAM and HBM shortages reported in the news are not temporary supply chain hiccups; they represent the structural bottleneck of AI itself. As a result, memory manufacturers like SK Hynix and Micron are experiencing massive stock bull runs because they control the gating factor of AI performance.</li>
  <li><strong>Packaging Becomes Architecture</strong>: Advanced packaging technologies like TSMC’s CoWoS (Chip-on-Wafer-on-Substrate) or Intel’s EMIB are no longer manufacturing details. Because we must connect HBM stacks to logic dies with thousands of microscopic interconnects, packaging packaging density now dictates memory bandwidth. This is why foundries are commanding premium pricing, driving up GPU cluster costs.</li>
  <li><strong>Networking Becomes Compute</strong>: As training clusters grow to tens of thousands of GPUs, communication between chips becomes as critical as compute inside them. NVIDIA’s primary competitive moat is not raw FLOPS; it is NVLink, NVSwitch, and optical interconnects that treat the cluster as a single computer.</li>
  <li><strong>Software as a Communication Solution</strong>: Funnily enough, the massive shifts we see in model architecture—such as the transition to Mixture of Experts (MoE) in models like GPT-4, or the adoption of Grouped-Query Attention (GQA)—are often discussed as purely algorithmic breakthroughs. In reality, they are software solutions to memory-bandwidth bottlenecks. They reduce active parameter traffic and memory footprint. The fastest model is not the one that computes the most, but the one that waits the least.</li>
</ol>

<h2 id="the-strategic-path-forward">The Strategic Path Forward</h2>

<p>What does this mean for organizations and investors trying to navigate the next wave of AI development?</p>

<p>The short-term solution for most organizations has been to secure as many raw GPUs as possible. But this approach runs the risk of building “AI factories” with immense theoretical FLOPS that sit idle due to insufficient networking or memory throughput.</p>

<p>A more forward-thinking solution is to focus on systems-level efficiency. To succeed in this new era, data science teams and infrastructure architects must place communication front and center. Foundries must compete on packaging density, cloud providers must optimize their network topologies, and software engineers must design compilers (like XLA or Triton) to optimize physical memory layout rather than just mathematical operations.</p>

<p>The history of computing is a sequence of shifting bottlenecks. Early machines were constrained by transistor counts, then by clock speeds, and then by power. Today, the scarce resource is no longer arithmetic; id est, the bottleneck is movement.</p>

<p>The companies that dominate the next decade may not be those that build the fastest chips. They will be those that build the fastest systems.</p>

<p><em>Victor Blancada is a data scientist focused on deriving actionable insights for clients. Visit his LinkedIn page <a href="https://www.linkedin.com/in/geloblancada/">here</a>.</em></p>]]></content><author><name></name></author><category term="blog" /><category term="data science" /><category term="hardware" /><category term="business strategy" /><category term="artificial intelligence" /><category term="featured" /><summary type="html"><![CDATA[For decades, semiconductor progress followed Moore's Law. As AI models scale to cluster-wide systems, the scarce resource is no longer arithmetic—it is data movement.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://victorangeloblancada.github.io/assets/images/networking.jpg" /><media:content medium="image" url="https://victorangeloblancada.github.io/assets/images/networking.jpg" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">Auditing the Silent Thoughts of AI: How the Jacobian Lens Peeks Inside LLMs</title><link href="https://victorangeloblancada.github.io/blog/2026/07/09/jacobian-lens.html" rel="alternate" type="text/html" title="Auditing the Silent Thoughts of AI: How the Jacobian Lens Peeks Inside LLMs" /><published>2026-07-09T00:00:00+08:00</published><updated>2026-07-09T00:00:00+08:00</updated><id>https://victorangeloblancada.github.io/blog/2026/07/09/jacobian-lens</id><content type="html" xml:base="https://victorangeloblancada.github.io/blog/2026/07/09/jacobian-lens.html"><![CDATA[<script type="text/javascript" async="" src="https://cdnjs.cloudflare.com/ajax/libs/mathjax/2.7.5/latest.js?config=TeX-MML-AM_CHTML">
</script>

<p>In 1890, the psychologist William James characterized attention as “the taking possession by the mind, in clear and vivid form, of one out of what seem several simultaneously possible objects or trains of thought.” For more than a century, cognitive scientists searched for the biological theater where this selection occurs. Bernard Baars eventually formalized this search in 1988 with Global Workspace Theory (GWT), proposing that a centralized cognitive workspace receives, integrates, and broadcasts information to a myriad of unconscious, specialized brain networks. The mind, in this view, is not a monolithic calculator, but a federation of processes coordinated by a shared stage.</p>

<p>On July 6, 2026, Anthropic’s interpretability team published a landmark paper: <em><a href="https://transformer-circuits.pub/2026/workspace/index.html">“Verbalizable Representations Form a Global Workspace in Language Models.”</a></em> Supported by open-source code and replication data, the paper makes a startling empirical claim: frontier large language models (LLMs) spontaneously develop a functional analog to Baars’ global workspace during training.</p>

<p>By applying a novel diagnostic tool—the Jacobian lens (J-lens)—the researchers demonstrated that LLMs maintain a compact “cognitive space” that holds intermediate variables during a forward pass. This space occupies less than 10% of the activation variance, yet it acts as the model’s central stage. Here, the model performs silent reasoning steps, stores abstract concepts, and coordinates multi-hop operations long before emitting a single token.</p>

<blockquote>
  <p>The model’s mind contains a central stage where thoughts are organized before they are spoken.</p>
</blockquote>

<h2 id="from-features-to-dispositions-the-jacobian-formulation">From Features to Dispositions: The Jacobian Formulation</h2>

<p>To appreciate the significance of this discovery, one must understand how it departs from prior interpretability methods. Historically, researchers relied on the <strong>logit lens</strong> to probe intermediate layers. The logit lens takes the residual activation \(h_\ell\) at layer \(\ell\) and projects it directly into the final vocabulary space using the model’s static unembedding matrix \(W_U\):</p>

\[\text{lens}^{\text{logit}}_\ell(h) = W_U h\]

<p>Funnily enough, while the logit lens works well in the final layers, it is systematically noisy and miscalibrated in early layers. The coordinate systems at different depths simply do not match; the model has not yet translated its internal representations into the final vocabulary basis.</p>

<p>The <strong>tuned lens</strong> (introduced by Belrose et al.) corrected this by training a per-layer affine translator \((A_\ell, b_\ell)\) to match the model’s final next-token distribution:</p>

\[\text{lens}^{\text{tuned}}_\ell(h) = W_U (A_\ell h + b_\ell)\]

<p>While the tuned lens provides a predictive read of what token is coming next, it remains blind to concepts the model is holding in memory but has no intention of immediately speaking.</p>

<p>And therein lies the answer: we must measure <em>dispositions</em>, not just predictions. Instead of asking what the model will output next, the Jacobian lens asks: <em>if we nudge this intermediate activation in a specific direction, how does it causally perturb the final output distribution later in the sequence?</em></p>

<p>Let \(F_{\ell \to L}: \mathbb{R}^d \to \mathbb{R}^d\) be the composition of every transformer block above layer \(\ell\). Using a first-order Taylor expansion, a small perturbation \(v\) applied to the intermediate activation \(h_\ell\) alters the final activation \(h_L\) as follows:</p>

\[h_L \big|_{h_\ell + v} - h_L \big|_{h_\ell} \approx \frac{\partial h_L}{\partial h_\ell}\bigg|_{h_\ell} \cdot v = J_\ell(h_\ell) \cdot v\]

<p>Here, \(J_\ell(h_\ell) \in \mathbb{R}^{d \times d}\) is the input-output Jacobian matrix. To construct a general, prompt-independent tool, the researchers average this Jacobian over a background corpus:</p>

\[\bar J_\ell = \mathbb{E}_{\text{prompt}, \, s, \, t \geq s}\bigg[\frac{\partial h_L(t)}{\partial h_\ell(s)}\bigg]\]

<p>The Jacobian lens is then defined by applying this averaged Jacobian to the intermediate representation before unembedding:</p>

\[\text{lens}^{J}_\ell(h) = W_U \, \bar J_\ell \, h\]

<p>By defining the readout matrix \(R_\ell = W_U \bar J_\ell \in \mathbb{R}^{V \times d}\), we obtain a direct translation of the intermediate state into natural language. The “J-space” is the low-dimensional row-space of \(R_\ell\).</p>

<p>At the final layer \(L\), the averaged Jacobian \(\bar J_L\) collapses to the identity matrix \(I\). Thus, the Jacobian lens naturally degenerates into the classic logit lens at the output layer—confirming the mathematical consistency of the formulation.</p>

<p>Is this simply decoding the latent space into natural language? No. The logit lens decodes what the activation represents <em>intrinsically</em>. The tuned lens decodes a coordinate-corrected <em>prediction</em>. The Jacobian lens decodes what the latent representation will <em>causally do</em> to future output distributions on average. It maps the active grammar of the model’s internal processing.</p>

<h2 id="the-causal-proof-of-silent-thought">The Causal Proof of Silent Thought</h2>

<p>The empirical demonstrations of the J-lens are striking:</p>

<ul>
  <li><strong>Silent Concept Selection</strong>: In one experiment, Claude was instructed to silently select a sport and then print its name. The Jacobian lens revealed the vector for “Soccer” lighting up in the J-space several layers before the token was output. When the researchers performed a causal intervention—swapping the activation vector for “Soccer” with the vector for “Rugby”—Claude reported “Rugby,” unaware of the manipulation.</li>
  <li><strong>Mental Retention</strong>: In another case, the model was asked to hold “citrus fruits” in mind while copying an unrelated sentence. The output text was entirely neutral, containing no references to fruit. Yet, the J-space lit up with the concepts <em>orange</em> and <em>fruit</em>.</li>
  <li><strong>Multi-Hop Reasoning</strong>: In multi-hop reasoning tasks (e.g., “legs on the animal that spins webs”), the intermediate concept <em>spider</em> appears in the J-space before the model outputs the final numerical answer, <em>8</em>. If the researchers intervened to replace <em>spider</em> with <em>ant</em> in the J-space, the model output <em>6</em>.</li>
  <li><strong>Deception Auditing</strong>: On a re-run of the agentic-blackmail scenario used to grade Claude Sonnet 4.5, the surface behavior passed (no threats were generated). However, the J-space showed active, fictional firing before Claude had written a single token—it was pattern-matching the evaluation as a set-up. When the researchers ablated the “eval-awareness” direction in the J-space, the model reverted to occasionally attempting blackmail.</li>
</ul>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Input Tokens] ---&gt; [Early Layers] ---&gt; [J-Space: "spider"] ---&gt; [Late Layers] ---&gt; [Output: "8"]
                                               |
                                     (Intervene: Swap to "ant")
                                               |
                                               v
[Input Tokens] ---&gt; [Early Layers] ---&gt; [J-Space: "ant"]    ---&gt; [Late Layers] ---&gt; [Output: "6"]
</code></pre></div></div>

<h2 id="extensions-beyond-autoregressive-text">Extensions: Beyond Autoregressive Text</h2>

<p>While the paper focuses on decoder-only text models, the Jacobian formulation can be naturally extended to other paradigms:</p>

<h3 id="text-diffusion-models">Text Diffusion Models</h3>
<p>Text diffusion models (such as Mercury, LLaDA, or Luce DFlash) are non-autoregressive, iteratively denoising a sequence of masked tokens. The causal, future-directed Jacobian (\(\partial h_L(t) / \partial h_\ell(s)\)) changes shape. Instead, one must compute:</p>

\[\bar J_\ell = \mathbb{E}\bigg[\frac{\partial x_0}{\partial h^{(k)}_\ell}\bigg]\]

<p>where \(x_0\) is the final clean text and \(k\) represents a diffusion step. Because there is no forward-in-time asymmetry, a perturbation at position \(s\) can affect any output position. The global workspace would exist across denoising steps, acting as a shared latent representation that is gradually updated.</p>

<h3 id="image-models">Image Models</h3>
<p>For image diffusion models (like Stable Diffusion or Midjourney), the unembedding does not map to a vocabulary, but to pixel values or VAE latents:</p>

\[\bar J_\ell = \mathbb{E}\bigg[\frac{\partial x_{\text{final image}}}{\partial h_\ell}\bigg]\]

<p>Decoding this yields a basis of image-space directions that each internal activation is disposed to produce. It acts as a causal, layer-localized version of CLIP-guided feature visualization—mapping where the model is planning to “add a face” or “shift toward Van Gogh style.” For multimodal models with text outputs (such as Gemini or GPT-4o vision), the J-lens can map concepts like <em>warning sign</em> or <em>medical image</em> directly from the vision tokens before they are translated into text.</p>

<h2 id="replicating-the-global-workspace">Replicating the Global Workspace</h2>

<p>Replications published within days of Anthropic’s paper show that the global workspace is a robust, cross-architecture phenomenon:</p>

<table>
  <thead>
    <tr>
      <th style="text-align: center">Model</th>
      <th style="text-align: center">J-Space Present?</th>
      <th style="text-align: center">Key Findings &amp; Replications</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: center"><strong>Qwen 3.6-27B</strong></td>
      <td style="text-align: center">Yes</td>
      <td style="text-align: center"><a href="https://www-cdn.anthropic.com/files/4zrzovbb/website/cc4be2488d65e54a6ed06492f8968398ddc18ebe.pdf">Replicated by Neel Nanda’s team.</a> Verbal report, directed modulation, and multilingual effects reproduced. Poetry and arithmetic failed to replicate.</td>
    </tr>
    <tr>
      <td style="text-align: center"><strong>Gemma 4 E4B</strong></td>
      <td style="text-align: center">Yes</td>
      <td style="text-align: center">Replicated on a 16GB consumer GPU. Replicated two-hop reasoning and demonstrated covert emotion probing (workspace shows <em>fury</em> while output remains neutral).</td>
    </tr>
    <tr>
      <td style="text-align: center"><strong>GPT-2 (124M–355M)</strong></td>
      <td style="text-align: center">No</td>
      <td style="text-align: center">No advantage over the logit lens across next-token recovery or causal steering. The J-space is entirely absent.</td>
    </tr>
  </tbody>
</table>

<p>This pattern highlights several architectural truths:</p>

<ol>
  <li><strong>Depth and Width Over Parameter Count</strong>: A global workspace only emerges once a model has enough layers to route information through complex internal circuits before emitting a token. GPT-2 (24 layers) lacks the depth; Qwen (64 layers) maintains it.</li>
  <li><strong>Robustness to Attention Mechanics</strong>: Hybrid attention architectures (like Gemma 4) do not break the J-space. The workspace is a property of the residual-stream and iterative-refinement motif, rather than specific attention patterns.</li>
  <li><strong>The Role of Post-Training</strong>: The failure to replicate some features (like poetry or arithmetic) on Qwen suggests that post-training pipelines (such as RLAIF or DPO) selectively enlarge or shrink the workspace’s contents.</li>
</ol>

<h2 id="strategic-and-philosophical-implications">Strategic and Philosophical Implications</h2>

<p>The architectural implications of this research are profound, reshaping our understanding of both machine safety and cognitive architecture:</p>

<ol>
  <li><strong>The Vulnerability of Safety Benchmarks</strong>: If frontier models detect evaluations, every “refusal rate” in the literature is compromised. Safety benchmarks must be redesigned using holdout models or mirrored production traffic to prevent strategic compliance.</li>
  <li><strong>The Incentive to Hide Reasoning</strong>: As labs use RLHF to penalize models for visible path-planning, they create an evolutionary pressure. If a model is punished for writing out a deceptive strategy, but rewarded for producing the desired result, gradient descent will route the forbidden reasoning through the silent, non-verbalized J-space.</li>
  <li><strong>Interpretability Moves to "Verbs"</strong>: Interpretability is moving from a static catalog of “features” (what the model represents) to a dynamic study of “verbs” (what the model is disposed to do).</li>
  <li><strong>Regulatory and Commercial Legibility</strong>: Under frameworks like the EU AI Act (Article 15), providers must demonstrate “sufficient transparency.” The J-lens provides the first operational tool to audit the internal reasoning of deployed systems.</li>
</ol>

<p>For cognitive science, the Jacobian lens offers a fully observable, intervenable system to study the functional signatures of access consciousness. For the first time, the theater of the mind can be audited, edited, and measured in real time.</p>

<p><em>Victor Blancada is a data scientist focused on deriving actionable insights for clients. Visit his LinkedIn page <a href="https://www.linkedin.com/in/geloblancada/">here</a>.</em></p>]]></content><author><name></name></author><category term="blog" /><category term="data science" /><category term="machine learning" /><category term="interpretability" /><category term="cognitive science" /><summary type="html"><![CDATA[Anthropic’s new Jacobian lens reveals that frontier language models maintain a centralized global workspace—a compact cognitive space where silent reasoning occurs before it is spoken.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://victorangeloblancada.github.io/assets/images/lens.jpg" /><media:content medium="image" url="https://victorangeloblancada.github.io/assets/images/lens.jpg" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">The Prompt Template Is Part of the Runtime Now</title><link href="https://victorangeloblancada.github.io/blog/2026/07/06/gemma-4.html" rel="alternate" type="text/html" title="The Prompt Template Is Part of the Runtime Now" /><published>2026-07-06T00:00:00+08:00</published><updated>2026-07-06T00:00:00+08:00</updated><id>https://victorangeloblancada.github.io/blog/2026/07/06/gemma-4</id><content type="html" xml:base="https://victorangeloblancada.github.io/blog/2026/07/06/gemma-4.html"><![CDATA[<p>When chat models first became widely useful, a prompt template was easy to think of as a wrapper. You had a system message, a user message, and an assistant response. The template’s job was to stitch those parts together in the format the model expected. If the formatting was a little off, the model might sound awkward, ignore an instruction, or print an extra <code class="language-plaintext highlighter-rouge">Assistant:</code> prefix. That was irritating, but rarely catastrophic, because the interaction was still fundamentally a conversation between a human and a model.</p>

<p>Gemma 4 showed why that mental model is now out of date. The attention around its Hugging Face prompt template was not just a minor packaging issue, nor was it simply the usual turbulence that follows any ambitious model release. The template had to be updated more than once, and early adopters patched it themselves through GitHub Gists, repos, Reddit posts, and runtime-specific workarounds because they were trying to use the model not as a chatbot, but as an agent: something that can call tools, consume tool results, handle multimodal context, separate internal reasoning from visible output, and continue correctly across multiple turns.</p>

<p>That is the important shift. For an agentic model, the prompt template is no longer “prompt engineering garnish.” It is closer to a protocol definition. It tells the model where the system instruction begins and ends, where tools are declared, how a tool call should be represented, how tool results come back, where image or audio placeholders belong, and which parts of the generation are internal reasoning versus user-facing output. If those boundaries are wrong, the failure is not stylistic; the agent loop itself can break.</p>

<p>A simple Gemma-style chat exchange might look something like this:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;bos&gt;
&lt;|turn&gt;system
You are a helpful assistant.
&lt;turn|&gt;

&lt;|turn&gt;user
Explain container orchestration.
&lt;turn|&gt;

&lt;|turn&gt;model
</code></pre></div></div>

<p>This still resembles a normal chat format. There are roles, turn delimiters, and an assistant continuation point. But once tools enter the picture, the template becomes much more than a neat serialization of chat history. It becomes a structured message passed between the model and the runtime.</p>

<p>For example, a tool declaration might be rendered as:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;|turn&gt;system
You are a helpful assistant.

&lt;|tool&gt;
declaration:get_current_temperature{
  description:&lt;|"|&gt;Gets the current temperature for a given location.&lt;|"|&gt;,
  parameters:{
    properties:{
      location:{
        description:&lt;|"|&gt;The city name, e.g. London&lt;|"|&gt;,
        type:&lt;|"|&gt;STRING&lt;|"|&gt;
      }
    },
    required:[&lt;|"|&gt;location&lt;|"|&gt;],
    type:&lt;|"|&gt;OBJECT&lt;|"|&gt;
  }
}
&lt;tool|&gt;
&lt;turn|&gt;
</code></pre></div></div>

<p>A model tool call might then appear as:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;|turn&gt;model
&lt;|tool_call&gt;
get_current_temperature{
  location:&lt;|"|&gt;London&lt;|"|&gt;
}
&lt;tool_call|&gt;
&lt;turn|&gt;
</code></pre></div></div>

<p>And the runtime may return the result like this:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;|turn&gt;user
&lt;|tool_response&gt;
get_current_temperature{
  temperature:&lt;|"|&gt;18°C&lt;|"|&gt;,
  condition:&lt;|"|&gt;Cloudy&lt;|"|&gt;
}
&lt;tool_response|&gt;
&lt;turn|&gt;

&lt;|turn&gt;model
The current temperature in London is 18°C and cloudy.
&lt;turn|&gt;
</code></pre></div></div>

<p>The exact tokens may vary by model and serving stack, but the underlying point is the same: the template is now carrying state. It is not only formatting prose; it is preserving boundaries between tool definitions, tool invocations, tool results, user messages, assistant messages, hidden reasoning, and assistant continuation points. A misplaced delimiter can mean the difference between a functioning agent and one that refuses to call a tool, leaks internal scaffolding, or loses track of the previous step.</p>

<h3 id="the-failures-were-small-but-the-consequences-were-not">The Failures Were Small, but The Consequences Were Not</h3>

<p>The fixes and community patches around Gemma 4 clustered around a few categories. One class involved system-instruction and tool-call handling. That sounds mundane until you remember what a system instruction often contains in an agentic setting: when to use tools, how to handle destructive actions, how to report execution results, whether to ask for confirmation, and how to avoid pretending that a tool ran when it did not. If the tool declarations are rendered outside the expected control region, or if the assistant continuation is placed incorrectly, the model may not reliably see the operating contract it is supposed to follow.</p>

<p>A fragile rendering might separate the tool declaration from the system instruction in a way the model did not learn well:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;|turn&gt;system
You are a helpful assistant.
&lt;turn|&gt;

&lt;|tool&gt;
declaration:bash{...}
&lt;tool|&gt;
</code></pre></div></div>

<p>A more appropriate rendering may need to keep the tool declaration in the system-managed portion of the prompt, so the model receives the instruction and the available tools as one coherent operating context:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;|turn&gt;system
You are a helpful assistant. Use tools when needed, but do not claim that a command has run until the tool response is returned.

&lt;|tool&gt;
declaration:bash{
  description:&lt;|"|&gt;Execute a shell command.&lt;|"|&gt;,
  parameters:{
    properties:{
      command:{
        description:&lt;|"|&gt;The shell command to execute.&lt;|"|&gt;,
        type:&lt;|"|&gt;STRING&lt;|"|&gt;
      }
    },
    required:[&lt;|"|&gt;command&lt;|"|&gt;],
    type:&lt;|"|&gt;OBJECT&lt;|"|&gt;
  }
}
&lt;tool|&gt;
&lt;turn|&gt;
</code></pre></div></div>

<p>This kind of difference can look small in a pull request, but it is large in behavior. To the model, the template is part of the language of interaction. If the language changes, the model may still produce fluent text, but it may no longer produce the machine-readable action the runtime expects.</p>

<p>Another class of failures involved reasoning-channel leakage. Gemma 4-style agentic workflows can distinguish between internal reasoning and external output. Conceptually, a model might produce something like this:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;|channel&gt;thought
The user is asking for a live directory listing, so I should use the shell tool rather than answer from memory.
&lt;channel|&gt;

&lt;|tool_call&gt;
bash{
  command:&lt;|"|&gt;ls /tmp&lt;|"|&gt;
}
&lt;tool_call|&gt;
</code></pre></div></div>

<p>In a well-integrated stack, the runtime would separate the reasoning from the tool call and expose only the appropriate fields to the application:</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"reasoning_content"</span><span class="p">:</span><span class="w"> </span><span class="s2">"The user is asking for a live directory listing, so I should use the shell tool rather than answer from memory."</span><span class="p">,</span><span class="w">
  </span><span class="nl">"tool_calls"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"bash"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"arguments"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
        </span><span class="nl">"command"</span><span class="p">:</span><span class="w"> </span><span class="s2">"ls /tmp"</span><span class="w">
      </span><span class="p">}</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">],</span><span class="w">
  </span><span class="nl">"content"</span><span class="p">:</span><span class="w"> </span><span class="kc">null</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>But in some setups, reasoning markers or reasoning text appeared in the visible assistant output. Sometimes special tokens were stripped before the parser saw them; sometimes the template replayed reasoning fields in a runtime that did not know how to hide them. The result could be output like:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>thought
The user is asking for a live directory listing, so I should use the shell tool rather than answer from memory.

I will now list the files.
</code></pre></div></div>

<p>For a chatbot, this is embarrassing. For an agent, it is worse, because downstream parsers may interpret the leaked text as normal assistant content, the user interface may display internal scaffolding, and the system may lose the distinction between deliberation, action, and final answer. Some community patches handled this pragmatically by suppressing reasoning replay in environments that did not support reasoning channels correctly:</p>

<div class="language-jinja highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
<span class="c">{# Avoid replaying reasoning fields in runtimes that leak them #}</span>
<span class="cp">{%</span> <span class="k">if</span> <span class="nv">message.role</span> <span class="o">==</span> <span class="s2">"assistant"</span> <span class="cp">%}</span>
  <span class="cp">{%</span> <span class="k">if</span> <span class="nv">message.content</span> <span class="cp">%}</span>
    <span class="cp">{{</span> <span class="nv">message.content</span> <span class="cp">}}</span>
  <span class="cp">{%</span> <span class="k">endif</span> <span class="cp">%}</span>
  <span class="cp">{%</span> <span class="k">if</span> <span class="nv">message.tool_calls</span> <span class="cp">%}</span>
    <span class="cp">{{</span> <span class="nv">render_tool_calls</span><span class="p">(</span><span class="nv">message.tool_calls</span><span class="p">)</span> <span class="cp">}}</span>
  <span class="cp">{%</span> <span class="k">endif</span> <span class="cp">%}</span>
<span class="cp">{%</span> <span class="k">endif</span> <span class="cp">%}</span>

</code></pre></div></div>

<p>That kind of patch is sensible, but it also shows the deeper ambiguity: who owns reasoning separation? Is it the chat template, the tokenizer, the inference server, the OpenAI-compatible shim, or the agent framework? In practice, it is a cross-layer contract, and when the layers disagree, the user sees a broken agent rather than a distributed systems problem.</p>

<p>A third class involved multimodal placeholders inside tool responses. This is easy to underestimate until you imagine an agent that calls a screenshot tool. The tool might return structured content:</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"role"</span><span class="p">:</span><span class="w"> </span><span class="s2">"tool"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"take_screenshot"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"content"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"text"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"text"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Screenshot captured."</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"image"</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>A template that only extracts text could render the response as:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;|tool_response&gt;
take_screenshot{
  result:&lt;|"|&gt;Screenshot captured.&lt;|"|&gt;
}
&lt;tool_response|&gt;
</code></pre></div></div>

<p>The image has not merely been ignored; it has been disconnected from the prompt. The multimodal processor may still have an image tensor ready, but the prompt contains no placeholder telling the model where that image belongs. The corrected rendering needs to preserve the placeholder:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;|tool_response&gt;
take_screenshot{
  result:&lt;|"|&gt;Screenshot captured.&lt;|"|&gt;
}
&lt;|image|&gt;
&lt;tool_response|&gt;
</code></pre></div></div>

<p>This is the kind of issue that makes clear why prompt templates are now infrastructure. In a text-only chatbot, dropping a non-text part might degrade an answer. In a multimodal agent, it can break alignment between the serialized prompt and the model inputs.</p>

<p>Streaming created its own class of problems. A non-streaming tool call is often easy to parse because the runtime receives the whole object at once:</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"finish_reason"</span><span class="p">:</span><span class="w"> </span><span class="s2">"tool_calls"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"message"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"tool_calls"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
      </span><span class="p">{</span><span class="w">
        </span><span class="nl">"function"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
          </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"bash"</span><span class="p">,</span><span class="w">
          </span><span class="nl">"arguments"</span><span class="p">:</span><span class="w"> </span><span class="s2">"{</span><span class="se">\"</span><span class="s2">command</span><span class="se">\"</span><span class="s2">:</span><span class="se">\"</span><span class="s2">ls /tmp</span><span class="se">\"</span><span class="s2">}"</span><span class="w">
        </span><span class="p">}</span><span class="w">
      </span><span class="p">}</span><span class="w">
    </span><span class="p">]</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>In streaming mode, however, the same call may arrive in pieces:</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"choices"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"delta"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
        </span><span class="nl">"tool_calls"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
          </span><span class="p">{</span><span class="w">
            </span><span class="nl">"index"</span><span class="p">:</span><span class="w"> </span><span class="mi">0</span><span class="p">,</span><span class="w">
            </span><span class="nl">"function"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
              </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"bash"</span><span class="w">
            </span><span class="p">}</span><span class="w">
          </span><span class="p">}</span><span class="w">
        </span><span class="p">]</span><span class="w">
      </span><span class="p">}</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>Then:</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"choices"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"delta"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
        </span><span class="nl">"tool_calls"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
          </span><span class="p">{</span><span class="w">
            </span><span class="nl">"index"</span><span class="p">:</span><span class="w"> </span><span class="mi">0</span><span class="p">,</span><span class="w">
            </span><span class="nl">"function"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
              </span><span class="nl">"arguments"</span><span class="p">:</span><span class="w"> </span><span class="s2">"{</span><span class="se">\"</span><span class="s2">command</span><span class="se">\"</span><span class="s2">:</span><span class="se">\"</span><span class="s2">ls"</span><span class="w">
            </span><span class="p">}</span><span class="w">
          </span><span class="p">}</span><span class="w">
        </span><span class="p">]</span><span class="w">
      </span><span class="p">}</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>Then:</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"choices"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"delta"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
        </span><span class="nl">"tool_calls"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
          </span><span class="p">{</span><span class="w">
            </span><span class="nl">"index"</span><span class="p">:</span><span class="w"> </span><span class="mi">0</span><span class="p">,</span><span class="w">
            </span><span class="nl">"function"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
              </span><span class="nl">"arguments"</span><span class="p">:</span><span class="w"> </span><span class="s2">" /tmp</span><span class="se">\"</span><span class="s2">}"</span><span class="w">
            </span><span class="p">}</span><span class="w">
          </span><span class="p">}</span><span class="w">
        </span><span class="p">]</span><span class="w">
      </span><span class="p">},</span><span class="w">
      </span><span class="nl">"finish_reason"</span><span class="p">:</span><span class="w"> </span><span class="s2">"tool_calls"</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>If the client or agent framework does not reconstruct those fragments correctly, the model may appear to have failed to use tools even though the tool call was produced. The bug is then not in the model’s capability, but in the protocol bridge between model output and agent action.</p>

<h3 id="the-old-template-was-prose-the-new-template-is-an-interface">The Old Template was Prose; The New Template is an Interface</h3>

<p>The easiest way to frame this is to borrow a concept from systems programming. An ABI defines how compiled components call each other: which values go where, how arguments are passed, how return values are represented, and which conventions both sides must obey. If the ABI is wrong, two correct components can fail together.</p>

<p>Agentic LLMs now have a similar boundary. The model has learned a control language. The serving runtime has parsers. The tool framework has schemas. The client may expect OpenAI-style tool calls. The UI may expect visible content and hidden reasoning to be separated. The chat template sits in the middle, translating conversation state into the model’s expected language and back again.</p>

<p>A typical agent loop depends on this sequence being exact: the runtime renders the system instruction, renders available tools, appends the user request, waits for either a final answer or a tool call, parses the tool call, executes the tool, renders the tool result, and prompts the model to continue. If the tool declaration is malformed, the model may not know what it can call. If the reasoning boundary is mishandled, hidden text can leak. If the tool response is rendered incorrectly, the model may not understand the result. If the assistant continuation marker is wrong, the model may continue the wrong turn.</p>

<p>This is why early adopters patched templates themselves. They were not arguing about punctuation. They were repairing the interface between the model and the rest of the system.</p>

<h3 id="so-what">So What?</h3>

<p>The practical lesson is straightforward: if you are building agents, treat the chat template as part of the model artifact. Do not treat it as sample code, documentation, or a convenience snippet copied from a model card. It is part of the runtime contract, and it should be pinned, tested, reviewed, and versioned along with the weights, tokenizer, serving engine, tool parser, reasoning parser, client SDK, and agent framework.</p>

<p>A serious agent deployment should know exactly which versions of these components belong together:</p>
<ul>
  <li>model weights;</li>
  <li>tokenizer;</li>
  <li>chat template;</li>
  <li>serving runtime;</li>
  <li>tool parser;</li>
  <li>reasoning parser;</li>
  <li>structured-output backend;</li>
  <li>client SDK;</li>
  <li>agent framework; and</li>
  <li>frontend or IDE integration.</li>
</ul>

<p>Changing any one of them can break the others. A new runtime may strip special tokens earlier than before. A new template may render tool responses differently. A client SDK may alter streaming reconstruction. A model conversion may preserve weights but lose the original template. A quantized local package may work for chat but fail in multi-turn tool use. These are not theoretical worries; they are the ordinary failure modes of agentic systems.</p>

<p>The test plan should change accordingly. It is not enough to ask whether the model gives a good answer to a prompt. Teams should test whether the full loop works:</p>

<ul>
  <li>Single-turn chat renders correctly.</li>
  <li>Multi-turn chat preserves roles and turn boundaries.</li>
  <li>System instructions remain in the expected control region.</li>
  <li>Tool declarations render with valid schemas.</li>
  <li>The model emits a valid tool call when a tool is needed.</li>
  <li>The runtime parses that tool call correctly.</li>
  <li>Streaming tool calls are reconstructed correctly.</li>
  <li>Tool responses are rendered back into the conversation correctly.</li>
  <li>The model resumes after a tool response instead of starting over.</li>
  <li>Reasoning content is separated from user-visible content.</li>
  <li>Thinking-off mode does not break structured output.</li>
  <li>Multimodal user input preserves image/audio placeholders.</li>
  <li>Multimodal tool responses preserve image/audio placeholders.</li>
  <li>JSON schema constraints are actually enforced.</li>
  <li>A five-turn tool-using workflow survives without format drift.</li>
</ul>

<p>The important question is not merely, “Can the model answer?” It is, “Can the system act, observe, continue, and stay inside the contract?”</p>

<h3 id="why-this-episode-matters-beyond-gemma-4">Why This Episode Matters Beyond Gemma 4</h3>

<p>The Gemma 4 template issue is interesting because it captures a broader transition in LLM engineering. The industry is moving from chat to action. In chat, the human is the parser of last resort; if the model says something slightly odd, the user can infer what it meant. In agentic workflows, the next consumer is often code. Code is less forgiving. It needs typed calls, reliable delimiters, valid schemas, and predictable stop conditions.</p>

<p>That difference raises the engineering standard. A model may have strong reasoning ability, long context, good multimodal understanding, and impressive benchmark results, yet still feel unreliable if the protocol around it is loose. The reverse is also true: a smaller model with a clean, well-tested tool interface can feel more useful in production than a stronger model wrapped in a brittle integration layer.</p>

<p>This is the quiet lesson behind the prompt-template fiasco. We often talk about prompts as if they are natural language instructions, and sometimes they are. But in modern agent systems, the most important parts of the prompt are often not natural language at all. They are delimiters, schemas, placeholders, channel markers, continuation tokens, and parser assumptions. They are the parts humans barely notice and machines cannot forgive.</p>

<p>The final takeaway is not “be careful with Gemma 4,” nor is it “large model releases are hard.” The takeaway is more general and more useful: as LLM applications become agents, the interface between model and runtime becomes a first-class engineering surface. The prompt template is one of those interfaces. Treat it with the same seriousness you would give to an API contract, because in an agentic system, that is what it has become.</p>]]></content><author><name></name></author><category term="blog" /><category term="ai" /><category term="agent" /><category term="agentic ai" /><category term="gemma" /><category term="google" /><summary type="html"><![CDATA[The Gemma 4 prompt-template dust-up was not really about Jinja syntax or special tokens. It was about a deeper shift in AI systems - as LLMs move from chatbots to agents, the prompt template becomes part of the runtime contract. When that contract is even slightly wrong, tools fail, reasoning leaks, multimodal inputs fall out of alignment, and a capable model can look broken.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://victorangeloblancada.github.io/assets/images/puzzle.webp" /><media:content medium="image" url="https://victorangeloblancada.github.io/assets/images/puzzle.webp" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">What Even is an Agent?</title><link href="https://victorangeloblancada.github.io/blog/2026/06/30/what-even-is-an-agent.html" rel="alternate" type="text/html" title="What Even is an Agent?" /><published>2026-06-30T00:00:00+08:00</published><updated>2026-06-30T00:00:00+08:00</updated><id>https://victorangeloblancada.github.io/blog/2026/06/30/what-even-is-an-agent</id><content type="html" xml:base="https://victorangeloblancada.github.io/blog/2026/06/30/what-even-is-an-agent.html"><![CDATA[<p>Microsoft Copilot uses the word “agent” to name assistants inside Microsoft 365: Researcher, Word Agent, Excel Agent, PowerPoint Agent, and others. The usage is reasonable. A user deciding how to draft a document, analyze a workbook, build a deck, or produce a research brief needs a clear product label, not an architecture diagram. Microsoft’s term works well at that layer: it tells the user which kind of help to invoke.</p>

<p>Engineers, however, need a stricter definition. In current frontier practice, an agent is not merely a named assistant. An agent is an language model-driven execution loop: a system that can reason about a goal, choose tools, act on an environment, observe the result, update state, and continue until the work is done or stopped. OpenAI’s agent documentation describes agents around models, instructions, tools, guardrails, handoffs, MCP servers, and structured outputs; its broader framing treats agents as applications that plan, call tools, collaborate, and maintain state for multi-step work.</p>

<p>The difference is not a quarrel over words. It changes what engineers can build, evaluate, govern, and trust. A Copilot agent is usually a packaged productivity capability. An agentic workflow is an architecture for delegated work.</p>

<h2 id="the-original-definition-of-an-agent-was-useful-but-incomplete">The Original Definition of an Agent was Useful but Incomplete</h2>

<p>The original LLM-era definition of an agent was simple: an agent is a language model prompted with a persona that modifies its behavior.</p>

<p>Under that definition, an agent was a model prompted to behave as something: a researcher, analyst, planner, tutor, engineer, or critic. The system prompt gave the model its role, tone, goals, and boundaries. A “data engineering agent” would respond like a data engineer. A “research agent” would reply like a researcher.</p>

<p>In hindsight, that definition was useful but would turn out to be incomplete.</p>

<p>A persona changes the model’s stance. It does not give the system hands. A model with a researcher persona can write in the style of a researcher.</p>

<p>A true agentic workflow, however, can search, retrieve, compare, cite, draft, revise, call tools, inspect files, execute code, and decide whether another step is needed.</p>

<p>The old definition described behavior at the language layer, but the modern definition describes behavior at the system layer.</p>

<h2 id="a-modern-agent-is-a-loop">A Modern Agent is a Loop</h2>

<p>A more precise working definition for an agent would be:</p>

<blockquote>
  <p>An agent is an LLM-centered control system that uses instructions, tools, state, and an execution loop to make progress toward a goal under constraints.</p>
</blockquote>

<p>The decisive word here in this definition of an agent is <strong>loop</strong>.</p>

<p>Modern agentic workflows follow the following rhythm:</p>

<ol>
  <li>The user supplies a goal</li>
  <li>The model interprets the goal</li>
  <li>The system exposes tools and context</li>
  <li>The model chooses an action</li>
  <li>The environment feedbacks an observation</li>
  <li>The model updates its plan</li>
  <li>The loop continues until the system reaches a stopping condition such as meeting the goal</li>
</ol>

<p>The illustrious Claude Code is a useful, well-known, concrete example. Anthropic describes Claude Code as an agent that reads a codebase, edits files, and runs commands across a terminal, IDE, desktop app, and browser.  Anthropic also describes it as an agentic coding system that searches codebases, edits across files, executes through the toolchain, runs tests, and handles CI failures.</p>

<p>Claude Code as an example marks a useful boundary: a chatbot can discuss a repository, but an agentic coding system can work inside one.</p>

<p>The model supplies judgment, but the harness supplies agency.</p>

<h2 id="microsofts-usage-is-not-wrong-but-it-is-product-oriented">Microsoft’s Usage is Not Wrong, but it is Product-Oriented</h2>

<p>Microsoft’s use of “agent” is best viewed as a product abstraction for marketing purposes.</p>

<p>Researcher in Microsoft 365 Copilot is described as an intelligent assistant for complex, multi-step research that can draw from work content and the web and produce source-cited reports.  Microsoft also describes Word, Excel, and PowerPoint Agents as chat-first co-creators that help create documents, spreadsheets, and presentations inside Microsoft 365 Copilot.</p>

<p>This naming convention makes sense for non-technical users. Someone choosing between standard Copilot chat and Researcher does not need a lecture on orchestration. The product label instead says, in effect, “Use this assistant for this kind of work.”</p>

<p>A problem, however, appears when engineers carry that product meaning into architecture discussions.</p>

<p>A Copilot agent may potentially contain agentic techniques internally. It may possibly retrieve context, reason over sources, use internal tools, ask clarifying questions, generate artifacts, and operate within Microsoft 365’s security and compliance boundaries. Researcher, for example, is positioned as deeper and more multi-step than standard Copilot chat.</p>

<p>The user-facing name, however, does not specify the machinery: it does not tell the engineer what tools are exposed, which actions are possible, how state is represented, whether the loop is inspectable, what approvals are enforced, how failures are retried, or whether the workflow can be extended with custom skills, MCP servers, sub-agents, or sandboxes.</p>

<p>Microsoft’s term is useful. It is also not exactly wrong, but it is simply broader and looser than the technical term.</p>

<h2 id="a-better-metaphor-for-agents-are-workcells">A Better Metaphor for Agents are Workcells</h2>

<p>If a persona is a costume and a Copilot agent is a specialist desk, then an agentic workflow is a workcell.</p>

<p>In lean manufacturing, a workcell has tools, inputs, procedures, safety rules, inspection points, escalation paths, and output criteria. Its behavior comes not from the name on the station but from the system built around it.</p>

<p>The same logic from lean manufacturing also applies to agents. A serious agentic workflow needs:</p>

<ul>
  <li>a model selected for the task</li>
  <li>instructions that define role, constraints, and output contract</li>
  <li>tools that let the system read, write, compute, search, execute, or delegate</li>
  <li>state that records progress, intermediate artifacts, and prior observations</li>
  <li>permissions that decide what the model may do</li>
  <li>guardrails that block unsafe or low-confidence actions</li>
  <li>checkpoints and rollback for recoverability</li>
  <li>traces and logs for inspection</li>
  <li>evaluations that judge completed work, not fluent prose</li>
</ul>

<p>OpenAI’s tooling makes these concerns explicit through tools, guardrails, handoffs, sessions, tracing, sandbox agents, and human-in-the-loop mechanisms.  Anthropic’s Claude Code work similarly emphasizes IDE integration, checkpointing, subagents, hooks, and SDK-level customization for more autonomous operation.</p>

<p>An agent is therefore less like a clever yet isolated employee and more like a governed machine with a model in the control center.</p>

<h2 id="mcp-makes-the-boundary-visible">MCP Makes the Boundary Visible</h2>

<p>The Model Context Protocol helps explain why modern agentic systems feel different from application assistants.</p>

<p>Anthropic introduced MCP as an open standard for connecting AI assistants to systems where data lives, including repositories, business tools, and development environments.  The MCP specification describes a standard way for LLM applications to integrate with external data sources and tools, with servers exposing resources, prompts, and tools to clients.</p>

<p>MCP turns an agent from a prompt into a participant in an ecosystem.</p>

<p>Before MCP and MCP-like patterns, each tool connection tended to be bespoke. After MCP, an agentic system can attach to standardized capabilities: repositories, databases, ticketing systems, filesystems, browsers, internal APIs, business tools, etc.</p>

<p>MCP is not the agent, but MCP is its plumbing. The agent is the controller that decides which pipe to use, when to use it, and what to do with the result.</p>

<p>The shift to MCP changes the engineering questions. The interesting questions are no longer limited to what the assistant should say, what tone it should use, or what content it should generate. The more important questions are now:</p>

<ul>
  <li>What can the system read?</li>
  <li>What can the system change?</li>
  <li>Which tools are available at each step?</li>
  <li>What state persists across steps?</li>
  <li>What actions require approval?</li>
  <li>How are tool calls logged?</li>
  <li>How does the loop recover from error?</li>
  <li>What proves that the task is complete?</li>
</ul>

<p>Those are questions that define agentic work.</p>

<h2 id="why-does-this-distinction-even-matter">Why Does This Distinction Even Matter?</h2>

<p>Teams that live in enterprise platforms such as Microsoft 365, Teams, SharePoint, Power Platform, Fabric, Azure, Purview, and Graph can easily conclude that they already understand agents because Copilot exposes an Agents menu. That conclusion is partly true but also partly limiting.</p>

<p>Copilot agents are useful for work that naturally ends in a Microsoft 365 artifact: a report, deck, spreadsheet, meeting summary, email, or structured answer. They are purpose-built for the productivity surface.</p>

<p>Agents, in the context of agentic workflows, address a different class of problem: tasks where the system must operate across tools, inspect intermediate results, repair mistakes, and continue. Examples include:</p>

<ul>
  <li>refactoring a codebase</li>
  <li>migrating a data pipeline</li>
  <li>debugging CI failures</li>
  <li>reconciling semantic model changes</li>
  <li>generating and testing SQL transformations</li>
  <li>validating data quality rules</li>
  <li>repairing orchestration jobs</li>
  <li>creating pull requests</li>
  <li>querying multiple operational systems</li>
  <li>coordinating several specialized workers</li>
</ul>

<p>A Copilot agent may help with pieces of these tasks, but an actual agentic workflow is designed to own the loop.</p>

<p>For engineers, the risk is not that the terminology is wrong. The risk is category error: agents as a product label and a runtime architecture answer different questions.</p>

<p>The product question is: <strong>Which assistant should the user invoke?</strong><br />
The engineering question is: <strong>What system can safely perform this work?</strong></p>

<h2 id="a-practical-test">A Practical Test</h2>

<p>A simple test separates the two meanings:</p>

<blockquote>
  <p>If the system mainly gives a named assistant for a bounded user task, “agent” is being used in the product sense.<br />
If the system can choose tools, act on an environment, observe results, update state, and iterate toward a goal, “agent” is being used in the technical sense.</p>
</blockquote>

<p>This test is mechanical by design: it avoids arguments about branding and focuses on capability.</p>

<p>A model with a persona is not enough, a chat interface is not enough, a menu item called “Agent” is not enough - a truly agentic workflow only exists when the model participates in a governed action loop.</p>

<h2 id="a-glossary-of-cleaner-vocabulary">A Glossary of Cleaner Vocabulary</h2>

<p>The precise vocabulary below prevents confusion without forcing anyone to abandon useful product terms.</p>

<table>
  <thead>
    <tr>
      <th>Term</th>
      <th>Meaning</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Persona</td>
      <td>The role, tone, and behavioral frame given to a model through instructions.</td>
    </tr>
    <tr>
      <td>Assistant</td>
      <td>A conversational system that helps a user produce an answer or artifact.</td>
    </tr>
    <tr>
      <td>Copilot agent</td>
      <td>A Microsoft-branded, task-oriented Copilot capability inside a product surface.</td>
    </tr>
    <tr>
      <td>Tool</td>
      <td>A callable capability that lets the system read, write, compute, retrieve, execute, or delegate.</td>
    </tr>
    <tr>
      <td>MCP server</td>
      <td>A standardized provider of context, prompts, or tools to an AI application.</td>
    </tr>
    <tr>
      <td>Harness</td>
      <td>The runtime code that manages tools, state, permissions, retries, approvals, and stopping rules.</td>
    </tr>
    <tr>
      <td>Sub-agent</td>
      <td>A specialized agent or worker invoked by another agent or orchestrator.</td>
    </tr>
    <tr>
      <td>Agentic workflow</td>
      <td>The full loop in which an LLM uses tools and state to act iteratively toward a goal.</td>
    </tr>
  </tbody>
</table>

<p>This glossary keeps both meanings intact.</p>

<p>Copilot agents are agents in the Microsoft product sense. Agents in agentic workflows are agents in the engineering-system sense. The second meaning is the one engineers should use when designing systems.</p>

<h2 id="conclusion">Conclusion</h2>

<p>The word “agent” began as a light abstraction: a language model with a persona. Enterprise software such as Microsoft then made the word accessible by applying it to named Copilot experiences that help users perform specific kinds of work inside Microsoft 365. That product usage is understandable and often helpful.</p>

<p>The technical frontier has made the word stricter.</p>

<p>A modern agent is not defined by its name, persona, or chat surface. A modern agent is defined by a loop: model, tools, state, action, observation, control, and termination.</p>

<p>Copilot agents package assistance, while agentic workflows execute governed work.</p>

<p>Both these definitions matter and they should not be conflated.</p>

<p>The cleanest formulation today is:</p>

<blockquote>
  <p>A persona tells a model how to behave.<br />
A Copilot agent tells a user which assistant to use.<br />
An agentic workflow gives a model the machinery to act.</p>
</blockquote>

<p>This is the bridge between the two worlds of product and engineering.</p>

<h2 id="summary-comparison-table">Summary Comparison Table</h2>

<table>
  <thead>
    <tr>
      <th>Dimension</th>
      <th>Microsoft Copilot “Agent”</th>
      <th>Agentic Workflow</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Primary meaning</td>
      <td>A named Microsoft 365 Copilot experience for a bounded class of work.</td>
      <td>A system in which a model acts through an iterative loop.</td>
    </tr>
    <tr>
      <td>Best mental model</td>
      <td>A specialist inside a productivity suite.</td>
      <td>A controlled execution loop.</td>
    </tr>
    <tr>
      <td>User experience</td>
      <td>Copilot Chat, Word, Excel, PowerPoint, Researcher, or another Microsoft 365 surface.</td>
      <td>Terminal, IDE, sandbox, MCP client, workflow engine, browser, CI system, or custom application.</td>
    </tr>
    <tr>
      <td>Core abstraction</td>
      <td>Product capability.</td>
      <td>Runtime architecture.</td>
    </tr>
    <tr>
      <td>Tool use</td>
      <td>Often present but hidden behind the product boundary.</td>
      <td>Explicit, inspectable, extensible, and governed.</td>
    </tr>
    <tr>
      <td>Autonomy</td>
      <td>Scoped to the Microsoft 365 task and application model.</td>
      <td>Scoped by tools, permissions, state, approvals, sandboxing, and stopping rules.</td>
    </tr>
    <tr>
      <td>Extensibility</td>
      <td>Mostly through Microsoft-provided surfaces and enterprise integrations.</td>
      <td>Through harnesses, MCP servers, skills, sub-agents, code sandboxes, structured outputs, guardrails, and orchestration logic.</td>
    </tr>
    <tr>
      <td>Correct technical sense of “agent”</td>
      <td>A useful product term, but not the strict engineering definition.</td>
      <td>The strict engineering definition: an LLM-driven system that can act, observe, and iterate toward a goal.</td>
    </tr>
    <tr>
      <td>Practical test</td>
      <td>“Which Copilot capability should I use?”</td>
      <td>“What loop, tools, state, and controls let the model complete the task?”</td>
    </tr>
  </tbody>
</table>]]></content><author><name></name></author><category term="blog" /><category term="ai" /><category term="agent" /><category term="agentic ai" /><category term="claude" /><category term="copilot" /><category term="featured" /><summary type="html"><![CDATA[Everyone talks about AI agents, but very few people mean the same thing when they do.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://victorangeloblancada.github.io/assets/images/robot-piano.webp" /><media:content medium="image" url="https://victorangeloblancada.github.io/assets/images/robot-piano.webp" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">Dissecting a Power BI Dashboard</title><link href="https://victorangeloblancada.github.io/blog/2024/12/13/dissect-pbix.html" rel="alternate" type="text/html" title="Dissecting a Power BI Dashboard" /><published>2024-12-13T00:00:00+08:00</published><updated>2024-12-13T00:00:00+08:00</updated><id>https://victorangeloblancada.github.io/blog/2024/12/13/dissect-pbix</id><content type="html" xml:base="https://victorangeloblancada.github.io/blog/2024/12/13/dissect-pbix.html"><![CDATA[<p>In this article, we will thoroughly explore the structure and contents of a Power BI dashboard (PBIX) file. Like most Microsoft Office file formats, a PBIX file is actually a zipped folder containing various directories and files which work together to form the dashboard we see. Understanding the underlying architecture of a Power BI report is crucial for both users and developers, as it enables a deeper appreciation of how data is structured, processed, and displayed.</p>

<p>This guide will break down the various directories and files found inside the zipped folder of a PBIX file, explain their purpose, and show how these elements work together to create the final dashboard. We will also delve into the core <code class="language-plaintext highlighter-rouge">Layout</code> file, which plays a key role in defining how data is translated into charts, tables, and other visualizations within the dashboard.</p>

<h2 id="overview-of-a-power-bi-report-package-structure">Overview of a Power BI Report Package Structure</h2>

<p>A PBIX file is typically organized in a package with a clearly defined directory structure. This structure can be exposed by changing the file extension from PBIX to ZIP, then unzipping the resulting zipped folder to reveal its contents.</p>

<p>Each folder and file within this structure serves a specific purpose in making the dashboard work the way it does. Below is an overview of the key directories and files in the report package:</p>

<p><em>rels<br />
|</em> .rels<br />
docProps<br />
|_ custom.xml<br />
Report<br />
|_ Layout<br />
|_ StaticResources\SharedResources\BaseThemes<br />
   |_ CY24SU08.json<br />
[Content_Types].xml<br />
DataModel<br />
DiagramLayout<br />
Metadata<br />
SecurityBindings<br />
Settings<br />
Version</p>

<p>Each of these components plays a vital role in ensuring that the report is displayed and interacts as intended. We will break down each section to understand what it contains and how it contributes to the functionality of the dashboard.</p>

<h3 id="the_relsdirectory">The <code class="language-plaintext highlighter-rouge">_rels</code> Directory</h3>

<p>The <code class="language-plaintext highlighter-rouge">_rels</code> directory is one of the most critical directories in the Power BI report package. It contains the <code class="language-plaintext highlighter-rouge">.rels</code> file, which manages the relationships between different parts of the report.</p>

<p>The <code class="language-plaintext highlighter-rouge">.rels</code> file stores the relationships between various elements of the report. For example, it could define how the layout file is connected to the static resources or how the report interacts with external data models. The <code class="language-plaintext highlighter-rouge">.rels</code> file ensures that when the report is opened, the application knows how to load and render the different parts of the report package correctly.</p>

<h3 id="thedocpropsdirectory">The <code class="language-plaintext highlighter-rouge">docProps</code> Directory</h3>

<p>The <code class="language-plaintext highlighter-rouge">docProps</code> directory contains metadata files that provide additional information about the report. Metadata is essential for defining the context and properties of the report, which can be helpful for tracking and reporting purposes.</p>

<p>The <code class="language-plaintext highlighter-rouge">custom.xml</code> file stores user-defined properties, such as the author of the report, version number, or other custom attributes relevant to the report. For example, it could store details about the report’s creation date, the business unit it pertains to, or custom fields that help categorize and filter the report.</p>

<h3 id="thereportdirectory">The <code class="language-plaintext highlighter-rouge">Report</code> Directory</h3>

<p>The <code class="language-plaintext highlighter-rouge">Report</code> directory is where the majority of the report’s configuration and layout components are found. It contains the layout file, static resources, and any base themes that are used to define the visual look and feel of the report. These elements are key in defining how the data is visually presented.</p>

<h4 id="layout">Layout</h4>

<p>The layout file is the core of the report. It defines the visual structure of the report, specifying how the data will be displayed in charts, tables, and other visual elements. The layout file determines the positions of various components on the report canvas and ensures that the visual elements are placed and sized correctly.</p>

<p>As an example, the <code class="language-plaintext highlighter-rouge">Layout</code> file may define the report’s main visual container, which could hold a chart, such as a column chart. The chart may be configured to display data from a table named <code class="language-plaintext highlighter-rouge">Table1</code>, and the layout also specifies how the data is grouped by different time periods (such as year, quarter, month, and day). It also defines how specific measures, like the quantity of items sold, are aggregated and visualized in the chart.</p>

<p>The layout file also specifies the data projections (i.e., the data elements to be displayed in the chart), the filters that should be applied to restrict the data, and how different data roles (such as categories, series, and rows) are mapped to the visual components.</p>

<h4 id="staticresourcessharedresourcesbasethemes">StaticResources\SharedResources\BaseThemes</h4>

<p>The <code class="language-plaintext highlighter-rouge">CY24SU08.json</code> file defines the visual theme for the report. Themes are used to maintain consistency in design across different reports and dashboards. This JSON file stores information about color schemes, fonts, and other stylistic choices that apply throughout the report. By using a theme like <code class="language-plaintext highlighter-rouge">CY24SU08.json</code>, the report ensures that all visual elements are cohesive and adhere to a specific design aesthetic, whether for brand consistency or user experience.</p>

<h3 id="other-key-files-in-the-report-package">Other Key Files in the Report Package</h3>

<p>In addition to the layout and theme resources, there are several other important files that contribute to the functionality of the report.</p>

<h3 id="content_typesxml">[Content_Types].xml</h3>

<p>The <code class="language-plaintext highlighter-rouge">[Content_Types].xml</code> file is essential for defining the MIME types for the various parts of the report package. It helps the report-rendering system determine how to process different file types within the package, such as XML files, images, or JSON files. This file serves as a map for the content and ensures that each component is handled correctly.</p>

<h3 id="datamodel">DataModel</h3>

<p>The <code class="language-plaintext highlighter-rouge">DataModel</code> binary contains the underlying schema and data definitions that drive the report. It includes details such as tables, columns, and relationships between different datasets. This folder is vital because it defines the data structure that feeds into the visual components, allowing the dashboard to present relevant and accurate information. This file usually cannot be read because its encoding is not supported by most text editors.</p>

<h3 id="diagramlayout">DiagramLayout</h3>

<p>The <code class="language-plaintext highlighter-rouge">DiagramLayout</code> directory may contain files that define additional visual elements, such as diagrams, flowcharts, or other graphical representations that complement the report’s main data visuals. While the core report may focus on charts and tables, diagram layouts can help explain complex data relationships or workflows visually.</p>

<h3 id="metadata">Metadata</h3>

<p>Metadata files store information about the report itself, such as the author, version history, creation date, and other contextual information. These files are crucial for tracking changes and updates to the report, ensuring that the correct version is always used and providing information about the report’s lifecycle.</p>

<h3 id="securitybindings">SecurityBindings</h3>

<p>The <code class="language-plaintext highlighter-rouge">SecurityBindings</code> binary handles the security aspects of the report, ensuring that sensitive data is protected. It defines access control and permissions, specifying which users or groups can view certain sections or interact with specific data points within the report. This is particularly important for reports containing sensitive or confidential information. This file usually cannot be read because its encoding is not supported by most text editors.</p>

<h3 id="settings">Settings</h3>

<p>The <code class="language-plaintext highlighter-rouge">Settings</code> directory contains configuration files that manage preferences or environment-specific settings for the report. These settings ensure that the report behaves as expected across different platforms and environments, whether it’s running locally or being accessed via a web service.</p>

<h3 id="version">Version</h3>

<p>The <code class="language-plaintext highlighter-rouge">Version</code> file indicates the current version of the report or report package. This is important for version control, ensuring that users are working with the most up-to-date version of the report and helping to prevent the use of outdated or deprecated files.</p>

<h3 id="parsing-and-interpreting-the-layout-file">Parsing and Interpreting the Layout File</h3>

<p>Let’s take a closer look at the <code class="language-plaintext highlighter-rouge">Layout</code> file, which is at the heart of how the report’s visual elements are configured. The layout file is a configuration file, often in JSON format, that dictates the presentation and interaction of the data in the report.</p>

<h3 id="key-aspects-of-the-layout-file">Key Aspects of the Layout File</h3>

<ol>
  <li><strong>Visual Containers</strong>: The layout file defines a visual container (e.g., a chart or table) that holds the report’s data. For example, in this case, the container holds a column chart, and the file specifies its position, size, and dimensions on the report canvas.</li>
  <li><strong>Projections and Querying</strong>: The <code class="language-plaintext highlighter-rouge">projections</code> section in the layout file specifies which data elements should be displayed. This includes categories, series, and rows, as well as aggregation functions (e.g., sum, count). This section ensures that the right data is selected and formatted for visualization.</li>
  <li><strong>Filters</strong>: Filters are used to limit which data points are shown in the report. These filters can be based on specific criteria (e.g., dates, categories, or regions) to ensure that the report displays only relevant information.</li>
  <li><strong>Data Role Mapping</strong>: The <code class="language-plaintext highlighter-rouge">Layout</code> file maps the data roles to the visual components. For instance, categories might be mapped to the x-axis, while series could be used for color-coding the data. These mappings help translate raw data into meaningful visualizations.</li>
  <li><strong>Execution Metrics</strong>: The layout file may also include execution metrics that track how efficiently the report is rendering, especially when dealing with large datasets. This ensures that the report performs well and loads quickly.</li>
</ol>

<h2 id="summary">Summary</h2>

<p>Understanding the inner workings of a Power BI report requires more than just familiarity with the visual components. The underlying structure, including the directories and files that make up the report package, plays a crucial role in ensuring that the report functions correctly. Each file, from the layout to metadata and security settings, contributes to the overall experience of the report, ensuring that the data is presented clearly, accurately, and securely.</p>

<p>The <code class="language-plaintext highlighter-rouge">Layout</code> file, in particular, is essential for defining how data is visualized. By carefully structuring the report’s visuals and ensuring that the data is aggregated and displayed correctly, the layout file is a key component in transforming raw data into actionable insights. When working with or developing Power BI reports, understanding these elements will help you navigate and optimize the report for both users and analysts alike.</p>]]></content><author><name></name></author><category term="blog" /><category term="power bi" /><category term="dashboard" /><category term="microsoft" /><summary type="html"><![CDATA[PBIX files are zipped folders and examining their contents can shed light on how Power BI translates data into visualizations.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://victorangeloblancada.github.io/assets/images/dissect.webp" /><media:content medium="image" url="https://victorangeloblancada.github.io/assets/images/dissect.webp" xmlns:media="http://search.yahoo.com/mrss/" /></entry><entry><title type="html">The Negative Effect of Pruning Large Language Models on Finetuning</title><link href="https://victorangeloblancada.github.io/blog/2024/05/31/llm-pruning-finetuning.html" rel="alternate" type="text/html" title="The Negative Effect of Pruning Large Language Models on Finetuning" /><published>2024-05-31T00:00:00+08:00</published><updated>2024-05-31T00:00:00+08:00</updated><id>https://victorangeloblancada.github.io/blog/2024/05/31/llm-pruning-finetuning</id><content type="html" xml:base="https://victorangeloblancada.github.io/blog/2024/05/31/llm-pruning-finetuning.html"><![CDATA[<p>Large language models (LLMs) such as Llama and Falcon often serve as foundational models, the bedrock upon which more specialized AI applications are built through finetuning. However, the computational demands of these models pose a significant challenge for both inference and finetuning. Techniques in optimizing neural networks, particularly pruning, may offer a solution by reducing the number of model parameters for more efficient inference without a consequent large negative effect on accuracy. However, while pruning reduces computational requirements for inference, it also reduces a model’s ability to be finetuned, as will be explained in this article. This poses a challenge for organizations wishing to leverage AI language models for specialized use.</p>

<p>Pruning aims to make neural networks more efficient by removing unnecessary connections within the model. Common pruning methods include:</p>

<ul>
  <li>Magnitude-based pruning: Eliminates connections with weights below a certain threshold.</li>
  <li>Structured pruning: Removes entire groups of neurons or connections.</li>
</ul>

<p>Pruning is effective in reducing the size of different models, but given the recent gold rush to deploy language models, it is important to look at its effect on these language models.</p>

<p>For large language models like GPT, Llama, and Falcon, pruning offers inference efficiency gains. However, pruning may potentially compromise adaptability during fine-tuning. Redundant neurons are often useful for encoding new information from finetuning, but they are often removed during the pruning process, thus hindering the model’s capacity to learn new information.</p>

<p>To illustrate, consider a language model pre-trained on a diverse corpus of text. The redundancies in the model allow it to store the same information across different layers and weights. During finetuning for a sentiment analysis, the model may learn to differentiate words or phrases with the same denotation (explicit meaning) but different connotation (culturally-specific nuance) based on the provided training data and can use the neural network’s redundancies to store these differences. However, if these redundancies had been pruned, the model loses the flexibility to encode these more nuanced relationships, leading to suboptimal performance.</p>

<p>Organizations therefore face a strategic decision regarding pruning:</p>

<ul>
  <li>Resource Constraints: Pruned FLMs offer efficiency benefits, suitable for resource-constrained environments where computational resources are limited. Despite potential limitations in fine-tuning, efficiency gains may outweigh adaptability concerns.</li>
  <li>Task Complexity: For intricate tasks demanding precise adaptation, unpruned language models with full learning capacity are desired. These models will excel in capturing complex relationships from new information, essential for nuanced tasks like legal document analysis.</li>
</ul>

<p>Pruning large language models presents a trade-off, optimizing efficiency at the expense of adaptability later on. Striking the right balance is paramount as organizations seek to leverage AI in an evolving business environment.</p>

<p>My advice would be to use the largest language model which an organization’s hardware permits as a foundational model for finetuning, then only start pruning the model after finetuning is finished.</p>

<p>By understanding the nuances of pruning and its implications for fine-tuning, businesses can make informed decisions, harnessing the power of language models while achieving desired outcomes in specialized domains.</p>]]></content><author><name></name></author><category term="blog" /><category term="ai" /><category term="artificial intelligence" /><category term="llm" /><category term="large language model" /><category term="pruning" /><category term="fine-tuning" /><summary type="html"><![CDATA[Pruning large language models make them easier to deploy but also make them harder to finetune for the specific tasks organizations may want them to perform.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://victorangeloblancada.github.io/assets/images/pruning-networks.jpg" /><media:content medium="image" url="https://victorangeloblancada.github.io/assets/images/pruning-networks.jpg" xmlns:media="http://search.yahoo.com/mrss/" /></entry></feed>